# Fushi 个人版本 Agent 入口

本项目是 Fushi（原 Hibiki）的 Windows 个人版本。用中文协作，先理解用户当前是在讨论、收集问题，还是授权实施；不要把每条反馈都变成一轮修改和编译。

## 阅读顺序

首次处理本项目时，依次完整阅读：

1. [AGENTS.md](AGENTS.md) 和 [CLAUDE.md](CLAUDE.md)：作者技术规则与代码地图。
2. [个人工作规则](docs/personal/PERSONAL_FORK_RULES.md)：本人的协作节奏、模型分工、验证与采用边界。
3. 目标目录及其父目录中更近的 `AGENTS.override.md` / `AGENTS.md`、`CLAUDE.md`，以及本任务确实触发的专项流程。

同一会话中，已完整读过且未变化的文件不用在每条命令前重读。切换分支/worktree、规则有改动或上下文缺失时，核对版本，补读差异或缺失部分。不要遍历无关模块、历史报告或所有专项文档。

换模型或接手未完成任务时，先按个人规则第 8 节核对交接基线。内嵌查词任务从 [当前交接单](docs/personal/GAL_LOOKUP_HANDOFF.md) 进入，再按需查源码；不要从聊天摘要自行重建方案。

## 执行原则

- 技术与数据约束以作者规则为准；个人工作节奏和 `custom` 的采用/推送边界以个人规则为准。先按职责消除表面冲突，只在仍有实质冲突时询问。
- 已授权的必要调查、实现、定向验证和本地提交继续完成；完整应用构建与游戏操作默认由用户负责。
- 子代理的模型选择与分工见个人规则第 4 节的模型表，入口不重复具体模型名。
- 只在独立 worktree 修改；未验收的游戏候选、废弃工作区和个人正式线各自保留，不因整理规则而混入功能改动。

此文件只做入口。长期偏好只在个人规则中维护；技术细节留在作者文档；任务结果写入对应任务记录。本入口的加载行为参考 [OpenAI 官方说明](https://developers.openai.com/codex/guides/agents-md/)。
