# CMake 构建产物（injector exe / hook dll 各 arch），不入库——按需本地/CI 构建分发。
build/
dist/
out/
# .NET (unity_audio_extract)
bin/
obj/
# Python 诊断/文档工具缓存
__pycache__/
*.py[cod]
# 任务内 Hook 证据台账；release 前仅将脱敏、验签后的最终台账显式复制到 evidence/。
.galhook-evidence/

build-x64/
build-x86/
