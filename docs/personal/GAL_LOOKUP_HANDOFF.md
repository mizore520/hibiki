# 内嵌查词当前交接

更新：2026-09-19，第十四轮收尾。完整历史见 [样本记录](LOOKUP_CALIBRATION_SAMPLES.md)。

启动交接补充：用户随后运行 BAT 在源码状态检查处失败，已定位并修复未跟踪中文文件的 Git 转义与 Windows PowerShell 编码问题（BUG-2548）。启动器两项定向测试、分析以及原工作区 CMD/936 下的同一状态读取语句通过。没有启动完整构建，用户可继续运行原 BAT；OCR/Magpie 源码及下列验收边界不变。

## 基线与阶段

- 唯一工作区 `.worktrees/lookup-calibration-samples-20260917`，分支 `codex/lookup-calibration-samples-20260917`。起点 `d72f47e751`；Magpie 鼠标修复 `cf8d802367`；本轮最终源码 **`01a4eaaa09`**。未合入 `custom`、未推送、未提 PR；用户要求等他说提才提。
- 完整 Windows x64 构建由用户运行原 BAT。源码和定向验证完成，尚未交付本轮 EXE；游戏悬停/点击保持 `implemented_unverified`。用户禁止控制电脑；此前只读坐标采样未点击、移动鼠标或切换窗口。
- 用户最新明确停止继续调整剩余图片，要求尽快收尾。下列未合格样本保留记录，不再在本轮扩展算法。

## 用户目测结果优先

按 `.codex-test/round14-review-images/gallery.png` 从上到下：**只有第1、3、4项符合用户要求**；第3/4项是同图不同选区。第2项虽然程序返回成功，用户判为不合格；第5/6/7项仍拒绝。不能把自动4/7写成用户验收4/7或跨游戏准确率。

用户强调首行「与第二行普通字格宽高相同，差异属于行首缩进。已撤回未提交的跨行开引号缩宽推断，支持有测量依据的非整数续行缩进（非负0至8格，受容量上限约束），不因行起点不同改变开引号字格。第6/7项的图仍未解决，不称“只是加小数缩进就修好了”，也不把未确定原因定性为用户已排除的不规则游戏。

## 本轮实现

- Magpie 实际版本 v0.12.1。依据官方光标实现，捕获时将源窗口系统坐标映射到显示光标坐标，悬停和点击共用公式；退出捕获或按下/抬起空间不一致时撤销旧命中。呈现 HWND 纳入缓存身份。30秒150次只读采样中仅3次透明捕获，不冒充点击验收。见 BUG-2541。
- 像素定位加入 OCR token 身份约束，避免把普通字关联到邻字笔画；证据不足保留原 OCR。低置信度标点尾行可用已有正文网格与粗位置继续拟合，仍检查原生字符索引、行号和位置。横线变体仅在 OCR/Hook 对应时归一化，避免横线被错配给闭引号；Hook 原文不变。
- 续行缩进在 Dart 档案、channel 与 native 共用有限小数值。旧整数档案兼容且未改写用户数据；旧版程序不能可靠消费新保存的小数缩进，不能靠旧 EXE 验收新档案。
- 独立工具失败归档补齐当次日志、原图、正文、ROI、最终原因、源码版本及算法指纹，保存失败明确提示；已部署用户指定外部目录，用户重开脚本服务加载。六项 Python 检查和独立审查通过，证据/备份在 `.codex-test/round14-image-lab/`。未跟踪 `fushi/tool/gal_lookup_image_lab/` 是原阶段桥接，未夹带提交。

## 验证与限制

- 新归档8条，排除故意错文1条后为7次真实输入、5张图。缓存回放自动4项成功，错文仍拒绝；用户目测仅1/3/4合格，见上文。原始失败及候选叠框未改写或上传，只留本机。
- 旧42图按图像哈希/样本身份核对，旧36项成功全部保留，无回退；本轮冻结现存47图共自动40项成功，新增5项与旧42分开计数。日志 `.codex-test/round14-replay-final.log`，冻结输入/输出 `round14-frozen-drafts/`、`round14-old-replay/`。早期并发回放共享预览输入产生无效结果，最终已串行执行，不能引用早期结果。
- OCR拟合44项通过（包含等大字格半格缩进、弱横线尾行、偏移负例、首尾位置）。像素/身份、profile、projection、image-fit相关套件已通过；中途的回放装载失败已移除旧基线混编依赖并重跑回放通过。证据 `round14-ocr-fractional-final.log` 与 `round14-replay-final.log`，不累加重复执行次数。
- Magpie 原生对象与4个gate通过：`round14-native-final.log`；几何策略、低层钩子对象原生验证沿用已通过结果。小数缩进新增 native 排版 **35 cases passed**，`flutter_window.cpp` 对象 `/W4 /WX` 编译通过（Luna执行）。本轮进行了实际diff独立审查，未发现P0–P2；不替代游戏验收。
- 新增/主体文件定向分析无问题：`round14-analyze-clean.log`。旧 image-fit 文件仅改两处数值转换，完整文件分析遗留10条原有大括号风格info（`round14-analyze-final.log`）；未扩范围清理。最初分析服务器性能缓存退出异常，改用任务隔离目录后完成。
- `bug.dart check` 基线已有15个重复号（2257–2270、2439），非本批新增；未整理无关编号，不能称全库检查通过。历史证据 `round13-bug-record-check.log`。

## 保留行为与下一步

Hook正文、仅校准时PP-OCRv6 small、严格用户选区、对话/旁白两个可选槽、统一格高及按游戏/类别隔离的特殊字符推进均保留。浅蓝悬停、窗口缩放恢复、词典定位、截图/制卡屏障和附着竞态修复沿用已有候选；用户历史反馈闪退已不再发生，仍不升级引擎支持状态。

用户运行本工作区 `启动Hibiki最新版.bat` 编译当前HEAD（包含源码 `01a4eaaa09`），只需一次：

1. 现有校准直接试普通窗口 → Magpie → 退出 → 再次开启。鼠标与浅蓝高亮/查词应对应，台词外不高亮；点击不推进，退出不需重启 Fushi。
2. 顺带试缩小/放大窗口后恢复、词典弹窗位置及一次制卡。无必要不重启游戏或重做全部校准。
3. 独立工具重开后可重新识别已认可的第1/3/4项；其余按用户要求本轮停止调整。失败仍自动归档。

点击推进或闪退时停止该项并记录时间；按需读取 `%TEMP%/hibiki_glookup.log` 尾部。未完成完整应用构建或真实游戏验收，不提PR。
