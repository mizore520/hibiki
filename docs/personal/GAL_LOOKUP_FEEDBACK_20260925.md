# 游戏内查词优化：本轮体验问题清单

更新：2026-09-25。当前阶段：用户集中体验、记录问题；尚未开始统一修复。

## 基线

- 候选工作区 `.worktrees/gal-lookup-optimize-20260925`，分支 `codex/gal-lookup-optimize-20260925`，起点 `custom` 的 `47396e2a2c`（已含 2026-09-24 采用的查词校准，见 [交接单](GAL_LOOKUP_HANDOFF.md)）。
- 最终目标：把查词改动更新到作者仓库的草稿 PR hajisensai/Fushi#1625（头分支 `origin/codex/attached-lookup-author-pr-20260923`，基于 `upstream/develop` 的 `bc11e91117`）。
- PR 分支与 `custom` 不同：`custom` 另含个人 Hook、漫画 OCR、Anki 查重等内容，同名文件也有差异。因此本轮查词改动与个人内容分开提交，搬运时只挑查词提交到 PR 分支，并在那边单独核对编译与定向测试。
- 推送 PR 分支、合入 `custom` 各自需要用户单独同意。

## 集中问题清单

体验期间只记录，不逐条开工。每条写现象、预期和证据（截图、游戏名、日志位置），未复现的如实标注。

| # | 现象 | 预期 | 证据 / 场景 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 校准界面（对话校准 / 旁白校准）不好看。截图里能看到：说明文字很多，底部说明和右侧面板重复；右侧高级选项默认展开且描述很长；旁白页没有样本时左侧一大片空白，只有「还没有样本」。以上是代理根据截图列出的，用户只说了「丑」，具体哪些要改待确认 | 界面简洁，主流程一眼能看懂 | 截图 `.codex-test/gal-lookup-feedback-20260925/1.webp`、`2.webp` | 待确认细节 |
| 2 | 校准高级选项里的「只用「」内台词」开关要去掉 | 删掉这个开关，改用作者已有的文本处理规则来过滤（作者的文本处理里已有「只保留「」内的文本」「字符串替换」等规则） | 开关代码在 `quotedTextOnly`（`gal_lookup_surface_profile.dart`、`gal_lookup_calibration_image_fit.dart` 等）。待查：查词字格用的是原始 Hook 文本还是处理后的文本；已存档案里开着这个开关的该怎么兼容 | 待修 |
| 3 | 校准后游戏内查词：鼠标移到台词上有高亮，但点击后有时不弹查词窗口，时灵时不灵，感觉卡。用户复现一次：点第一个词，弹出；换另一个词，前两次点击都没弹，第三次才弹 | 每次点到高亮的字都能稳定弹出 | リトルバスターズ！EX，截图 `3.webp`；出现没有规律，没有确切时间点 | 未定位 |
| 4 | 快速点击翻到下一句时，新台词上没有内嵌查词效果（没有高亮、不能点），要等一会才出现 | 快速翻页后，新台词的查词效果马上就绪 | リトルバスターズ！EX；用户描述，没有时间点。用户已确认是快速翻页，不是在同一句上连点查词 | 未定位；与第 3 条是否同源待查，不预设 |
| 5 | Hook 文本自带换行的游戏：用一句较短的台词校准，框也画得较小；之后遇到比这句长的台词，查词框对不上 | 校准一次后，同一对话框里的长句也能对上，不受校准用的那句长短影响 | 用户以前在「超时空恋人」遇到过。用户判断这是通用问题：只要游戏自己在文本里换行（`\n` 或引擎控制码），都可能出现，修复不针对单个游戏。另需核对：目前只有真正的 `\n` / `\r` 算换行；引擎控制码形式的换行如果被作者的文本处理删掉或替换，换行信息会丢失。代码核对（未实机复现）：样本里没有自然折行时，`gal_lookup_calibration_ocr.dart` 用「样本最长一行」和「橙色框宽度」中较大的那个作为每行宽度，用橙色框高度决定行数；运行时 `attached_text_layout.h` 会把超过这个宽度的行强行折到下一行，行数超出框高则 `grid_overflow_body_rect`，整句没有查词框 | 根因候选已从源码确认，待统一修改 |

## 体验结束后的处理顺序

1. 汇总清单，按 `tool/bug.dart` 登记，核实每条根因（区分画面识别、Hook 文本、换行布局、点击命中、输入拦截、崩溃等边界）。
2. 统一修改，查词提交与个人提交分开。
3. 定向验证后提交一个候选，交用户用工作区 BAT 编译和进游戏复测。
4. 用户验收后，经同意再合入 `custom`，并把查词提交搬到 PR 分支。
