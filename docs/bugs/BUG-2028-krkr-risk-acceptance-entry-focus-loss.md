## BUG-2028 · KRKR 风险确认入口在切回 Fushi 后消失
- **报告**：2026-09-02（用户：Wight）
- **真实性**：✅ 真 bug。KRKR 已建立 `kind=1/id=1` 原生 provider，并能在游戏内命中、画出选区；但未确认当前 exe 的非完整输入盾风险时，中央查词路由会在 `fushi/lib/src/lookup/gal_ingame_lookup_controller.dart:536` 因 `_providerAdmission=false` 拒收 hit。原工作台又只按瞬时 `needsRiskAcceptance` 显示确认按钮并把它放在整行最右端：游戏前台时按钮会被游戏 HWND 遮住，切回 Fushi 准备点击时 native `targetBackground` 事件把状态改成 `suspended`，按钮随即消失，形成无法完成确认的闭环。确认前被拒的 hit 已推进游标，不会在授权后重放。
- **[x] ① 已修复** — `fushi/lib/src/lookup/gal_attached_text_controller.dart:355` 把“本 exe 已请求风险确认”记录成独立、按 target 生命周期清理的请求 token，只在 `needsRiskAcceptance` 及其瞬时 `suspended` 阶段保持待确认；授权、清档、detach、verified/faulted shield 与 off/calibrating 状态仍会收紧或关闭入口，不绕过逐 exe 风险门。`fushi/lib/src/lookup/gal_attached_text_controller.dart:1146` 让对话框提交绑定打开时的 target/exe/hash/token，并改成持久化成功后才发布授权；同 key 完整 profile 写串行且带最新写序号，写失败补偿真实 `PreferencesRepository` 的 eager cache，旧确认不能覆盖后来的模式、样式、清档、校准或目标切换。`fushi/lib/src/pages/implementations/gal_attached_lookup_workbench.dart:40` 用持久请求显示待确认状态，并把“确认点击风险”固定到标题左侧；写入在途只隐藏重复提交按钮，不会误报“安全”。没有修改 KRKR TJS、原生点击返回值、原生 dismiss fence、hover 或选区生命周期，避免重现先前本地试改造成的点击穿透和人物资源变化清选区回归。
- **[x] ② 已加自动化测试** — `fushi/test/pages/gal_attached_lookup_workbench_test.dart:116` 从真实 KiriKiri provider pair（`kind=1/id=1/Ready`）进入风险待确认，再注入 `targetBackground`，断言 controller 虽转为 `suspended` 仍保留左侧确认入口且可以打开二次确认框；对话框打开后替换 target，再确认必须 no-op。`fushi/test/lookup/gal_attached_text_controller_test.dart:621` 参数化覆盖除用户已修 SGRE 外四个真正会发布 hit 的适配（KRKR、Ren'Py、Siglus、Leaf/AQUAPLUS），逐一验证 `needsRisk → 带/不带 provider 元数据的 targetBackground → 确认持久化 → Ready/Active → activeNative`；同文件还覆盖 stale token、持久化失败/cache 回滚、延迟写与清档/校准/样式竞态、旧 activation 取消及 off 写失败 fail-closed。聚焦执行 widget/controller 共 45 条通过；KRKR 源码守卫 126 条通过；Windows x64 Debug 增量 `INSTALL` 构建通过。
- **[x] ③ 已修复捕获设置模态框拦截** — 真机日志证明 KRKR hit 已连续到达 Dart，但全部在 `providerAdmission=false` 处被拒；同时可访问性树中仍有一个被游戏 HWND 遮住的 `GalCaptureSetupDialog`，它的透明 modal barrier 使底层“确认点击风险”按钮看得见却收不到点击。现在首次与 post-frame 二次调度都在风险待确认时禁止打开捕获设置；已打开的对话框会监听同一 `GalAttachedTextController`，并经现有一次性 dismiss 出口主动让位。真实 widget route 回归覆盖“捕获弹窗已开 → 风险请求到达 → 弹窗关闭 → 风险按钮可点 → 二次确认出现”，并覆盖 barrier/Esc 先关闭时不得二次 pop 工作台；聚焦 Flutter 回归共 62 条通过。Windows x64 Debug 增量 `INSTALL` 构建成功，同一构建已重新校验并安装 x86/x64 helper 到 `voice_hook/<arch>/`。本次仍未修改 KRKR TJS、原生点击返回值或原生选区生命周期；Flutter 捕获弹窗的关闭口增加了当前路由校验。
- **备注**：风险确认是安全边界，修复不会自动替用户接受。确认后必须回到游戏重新点击一次；确认前的旧点击按设计不重放。
