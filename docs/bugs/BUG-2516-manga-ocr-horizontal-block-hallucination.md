## BUG-2516 · 本地漫画 OCR 横排段落整段幻觉（扉页简介 / 人物介绍 / 作者栏）
- **报告**：2026-09-13（用户：「本地 OCR 模型很烂」，样本「週に一度クラスメイトを買う話」mihon 下载 844×1200）
- **真实性**：✅ 真 bug。沿真实代码路径复测：竖排正文块（第 4 页起）整块喂 manga-ocr 几乎全对；烂的集中在前 3 页**横排多行段落**——`manga_ocr_pipeline.dart` `processPage` 把每个检测块原样交给 `MangaOcrRecognizer.recognize`，`cropAndResizeForRecognition`（`manga_ocr_recognizer.dart`）把 800×190 的段落 squish 成 224×224，manga-ocr 对横排多行整段幻觉（第 2 页简介识别成「当たりは交際の初めにフルーキングを…」）。对拍证明切好行再喂 manga-ocr 照样幻觉（方案 E），横排行必须走 PP-OCRv6 rec；竖排块整块喂 manga-ocr 反而最好（PP det 切行会把一列切断）。chimahon 的「PP-OCRv6 manga」权重是 ncnn + Android 闭源 .so，不可移植；用官方 PP-OCRv6 small ONNX（Apache-2.0）。
- **[x] ① 已修复** — `766f31f0a7`：`packages/fushi_engine/lib/ocr/routing_ocr_recognizer.dart` 按块方向路由（宽 ≥ 高 → 块内 PP det 切行 → 振假名过滤 → 横行 PP rec / 竖行 manga-ocr；没检到行或空串回落整块 manga-ocr），竖排块路径逐字节不变；`ppocr_line_detector.dart` / `ppocr_line_recognizer.dart` 新增；清单 +3 文件（30MB，URL 钉 HF revision sha）。`b2a9cfc5e5`：unclip 偏移量按连通域像素数算（倾斜标题行外接矩形面积虚大会吞邻行）。真实模型复跑第 1 章 28 页 26 秒：第 2 页简介整段、人物介绍、作者栏、倾斜标题 logo 逐字全对；竖排正文与修前一致。
- **[x] ② 已加自动化测试** — `fushi/test/ocr/routing_ocr_recognizer_test.dart`（三分支 + 两种回落 + 竖行页面坐标换算）、`ppocr_line_detector_test.dart`（输入尺寸 / BGR 归一化 / DB 后处理 / 振假名过滤 / 阅读顺序 / 端到端反变换）、`ppocr_line_recognizer_test.dart`（yml 字典解析含 `''` 转义与不支持形式抛错 / 预处理宽度规则 / CTC / 词表维度校验）、`manga_ocr_model_manifest_test.dart`（7 文件、revision sha 不含 main、镜像候选保留 revision）。
- **备注**：已装用户需在设置里补下 3 个新文件（downloadAll 只下未就绪文件）；离线导入时 HF 原名 `inference.onnx`/`inference.yml` 按字节数唯一命中（`matchMangaOcrModelFile` 的 `sizeBytes` 回退）；指纹签名随清单变化，旧逐页缓存目录自然失效，已有 manga.json 不动。「コミュ力」被读成「コミュカ」（カ/力同形）是 PP rec 固有，横排路径接受。真机 app 侧（flutter_onnxruntime 插件会话）未复测，纯 Dart FFI 会话已复测。
