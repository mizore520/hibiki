## BUG-2672 · 特殊字符宽度无法自动验证而被要求手动排版
- **报告**：2026-09-18（用户：另一款游戏的长台词被要求手动排版）
- **真实性**：用户截图确认流程报告字符不支持；源码已确认该报错可以在测量图像前由文本资格检查触发。失败样本的原始 Hook 文本尚未核对，具体触发字符待确认。
- **[ ] ① 未修复** — 本轮按用户要求只记录，暂不修改生产代码。
- **[ ] ② 未加自动化测试** — 尚未取得可稳定复现的特殊字符输入，暂不新增测试。

### 用户现场

- 截图当前选中的是样本 4，可见三行正文；顶部错误指向样本 3。不能把当前显示的句子当作失败样本，也不能据此认定程序已经检测出正文区域。
- 校准提示明确要求手动排版，理由是样本含有“暂时无法验证宽度的字符”。这使自动测量无法给出可用的排版结果。
- 截图中的游戏设计包含透明/半透明台词区域和复杂背景，特殊字形与逐字宽度测量的兼容性需要单独确认。
- 本轮附件截图作为现象证据保留在对话中，未复制进仓库。

### 当前边界

候选 `fa5cf81e23` 的 `fushi/lib/src/lookup/gal_lookup_calibration_image_fit.dart:86` 使用 `_eligible` 检查非空、长度与字符范围；第 346 行起在测量图像前逐个检查所有样本，一条不合格即可中止整批。因此提示不代表真的调用字体测量后得到了未知宽度。

当前显示的圆点形符号仅是后续检查线索：例如 `●`（U+25CF）不在允许范围内，但截图不能证明样本 3 的 Hook 文本含有这个码点。应核对实际失败样本，避免凭当前画面确定触发字符。

另记交互问题：界面停留在样本 4，却要求用户处理样本 3，缺少直达失败样本及明确原因的引导。后续统一记录入口见 [本轮反馈汇总](../personal/gal-lookup-feedback-20260918.md)。

### 备注

本轮没有修改生产代码、没有新增自动化测试、没有重新编译或运行完整 BAT。
