## BUG-2535 · 校准验证完成后 Fushi 发生访问冲突闪退


### 2026-09-19 最新用户复测反馈

用户明确反馈：“现在不会闪退了，能用了”。记录为当前实际体验中闪退已消失、可以使用，不再沿用“当前必现”的状态。此次没有说明具体游戏、复测次数或 EXE 指纹，不扩展成两款游戏均完成全矩阵验收，也不据此追认历史 WER 调用栈。

用户同时要求本轮只记录；未做新的代码修改、测试或构建。以下为此前的实施和现场记录。

### 2026-09-19 实施更新（待实机验收）

源码候选：`cea2b6cafb`，原分支本地提交，未合入正式线。

用户已授权统一修复，确认运行原候选工作区 BAT，并报告 BGI 与另一款游戏均在三点验证结束时必现；私有 EXE 路径留在本机会话，不提交。

本批定位并修复一个与该切换时机一致、可由源码证明的越界：`AttachedTextSurfaceWindow::PositionSurface` 改成较小正文窗口后，立即用旧的全客户区 `clusters_` 执行 `RenderLayerBitmap`，配置模式 alpha mask 循环未裁剪坐标，可能写出新 DIB。现先撤销旧命中快照/几何，完成重建后再绘制；所有 mask 写入另经过 `attached_bitmap_bounds.h` 裁剪。没有崩溃 dump，不能断言历史 WER 必然就是此点。

新增执行回归 `attached_bitmap_bounds_test.cpp`：旧 1920×1080 字框写入 6×4 缓冲区、负坐标、完全越界及跨界，检查像素内容与前后保护字节。该回归、mouse/source guard 及 surface 单对象编译通过。用户原始三点完成路径尚未实机复验，因此保留候选状态。

以下为修复前收集记录，其中“只记录”描述当时阶段。

- **报告**：2026-09-18（用户：校准验证完成后 Fushi 闪退）
- **真实性**：✅ 真 bug；Windows WER 已记录 `0xc0000005`，但目前没有 dump/调用栈，不能把根因归到某个函数。
- **[ ] ① 根因未完全定位** — 本轮针对校准/词典交接增加了候选 HWND 退役、re-arm pending 闸门和物理鼠标尾部隔离；这些是生命周期加固，不等于已证明修复该崩溃。
- **[x] ② 已有自动化回归覆盖相关生命周期** — 原生字簇事务、鼠标 Hook 源码守卫、窗口销毁退役路径均在本轮定向测试中通过；仍缺少可稳定触发访问冲突的测试触发器。

### 现场证据

- **发生时间**：2026-09-18 06:21:21（Windows WER）。
- **进程**：`fushi.exe`。
- **异常**：`0xc0000005`（ACCESS_VIOLATION）。
- **崩溃模块/偏移**：`fushi.exe`，`0x000000000000d899`。
- **WER 报告 ID**：`322d5b5c-1067-4ff0-b4e0-ac7de9ba9590`。
- **崩溃前状态**：日志显示三点校准已完成，随后状态进入 `activeAttached`。

### 新一轮集中体验反馈

- 用户再次报告：在一款游戏中，依次点击“第一个、中间、最后一个”三个框，完成三点校准验证后，Fushi 随即闪退。
- 用户随后更换另一款游戏，仍出现上述验证完成后闪退的现象。本轮至少两款游戏受影响，不能按单款游戏特例处理；是否同一崩溃根因仍待日志核对。
- 用户进一步明确：在当前体验中，三点验证完成后的闪退必定发生，严重影响体验，并阻断正常游戏阶段的功能测试。按用户报告标记为当前必现阻断项；尚未经代理受控复现，不虚构触发次数或崩溃调用栈。后续统一修复优先恢复验证完成后的稳定运行。
- 本次游戏名称、实际运行 EXE 的构建版本、崩溃时间和新的 WER/dump 尚未核对；不能直接沿用上次的异常码、模块偏移或推断本次根因相同。
- 本轮先归入 BUG-2535 的再次发生记录。用户明确要求集中记录、后续统一处理；当前不调查、不改代码、不编译、不运行测试。

### 待统一处理的边界

候选代码已覆盖最接近的交接风险：弹窗关闭后的临时候选不会在 HWND 已销毁后继续唤醒，re-arm 在消息未处理前不会把物理点击透传给游戏。真实游戏仍需在新候选上验证；若再次闪退，需要保留新的 WER/dump，才能继续定位具体调用链。

本条记录仍保持未完全修复状态，不把静态测试通过写成崩溃已消失。
