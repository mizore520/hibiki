## BUG-2673 · 多行台词可见但校准框无法可靠对齐字形
- **报告**：2026-09-18（用户：另一款游戏的两行台词也无法对齐，要求先记录）
- **真实性**：待复现。用户截图确认两行正文可见，橙色框已大致覆盖正文，但自动校准未生成可用结果；目前没有该样本的原始 Hook 文本、测量结果与日志，不能判定是台词完整性、行数识别、区域选取、字宽测量还是校准门槛导致。
- **[ ] ① 未修复** — 本轮按用户要求只记录，暂不修改生产代码。
- **[ ] ② 未加自动化测试** — 尚未建立受控复现，暂不新增测试。

### 用户现场

- 截图显示一条两行日文台词，正文位于画面底部浅色半透明区域；橙色外框大致包住两行正文。
- 当前选择的是样本 1，标记为“用于校准”。界面顶部提示“请选择或采集一张完整的两行或三行台词；短句无法确定换行位置。”
- 用户报告“这个，也不知道为啥对齐不了”。画面已经有两行可见文字，但提示仍要求两行或三行完整台词；需后续核对程序实际用于拟合的文本和图像证据，不能仅凭截图认定行数误判。
- 框的设置显示左 457.01 px、上 933.94 px、宽 1166.39 px、高 127.38 px；样本原图尺寸显示为 2051 × 1155。这些只是当时界面状态，不代表经验证的正确坐标。
- 本轮附件截图作为现象证据保留在对话中，未复制进仓库。

### 2026-09-18 复核补充

- 画面为浅底深色字，两行都在橙框内。现有证据不足以把失败归因于人工拖框误差，也不应让用户继续靠逐字点中心补偿。
- 候选 `fa5cf81e23` 的 `fushi/lib/src/lookup/gal_lookup_calibration_image_fit.dart:355` 起依次尝试亮字和暗字检测；亮字路径若将所有训练样本测为一行，会记下 `onlySingleLines`。最后第 481 行在拟合失败时优先返回 `multiline_required`，暗字路径失败的原因可能被这条提示覆盖。
- 这确认了提示的产生条件，尚未复现该截图的像素输入。不能把“算法检测为单行”写成“用户采集的是单行”，也不能断言浅色背景就是本次唯一根因。
- 后续统一记录入口见 [本轮反馈汇总](../personal/gal-lookup-feedback-20260918.md)。

### 预期与后续调查

- 当画面已经有完整多行正文时，校准应能自动确定文字位置、字距、行距和换行，不应要求用户逐字手动点到几何中心。
- 若样本仍不能用于拟合，应说明缺少哪项证据，以及与画面“两行可见”的差异；不能只反复要求用户换一条多行台词。
- 与 BUG-2671 的跨游戏文字检测失败、BUG-2672 的特殊字符宽度无法验证一起集中归因，暂不推断三者共享根因。
- 后续评估通用文字区域检测或 OCR 辅助方案：Hook 继续提供文本真值，视觉模块负责位置与字迹区域，再做序列对齐。现有几何算法的泛化不足需以不同游戏、复杂背景、无底色、多行与特殊字形样本验证；尚未在线实测或引入任何第三方方案，不能宣称其准确度已满足本项目。

### 备注

本轮没有修改生产代码、没有新增自动化测试、没有重新编译或运行完整 BAT，也不要求用户立即重新校准或重复测试。
