## BUG-1776 · 字幕列表不折叠同文本时间相接的卡拉OK交替事件
- **报告**：2026-08-22（用户：截图，神稲ぼたん S01E08 OP，字幕列表同句歌词在 1:01/1:03/1:04/1:06 连出四行，1:14~1:16 另一组）
- **真实性**：✅ 真 bug。片源真值：卡拉OK交替特效把同句歌词拆成时间首尾相接、startMs 各异的四组事件（61.67/63.13/64.67/66.21s，每组两层 \clip 分屏、样式互换闪烁）。BUG-841 的去重键是 `(startMs, 文本)` 精确相等（`fushi/lib/src/media/video/video_subtitle_jump_panel.dart` `_dedupedRawIndexes`，修复前 ~1046），只折得掉同 start 的特效对，折不掉链式分段 → 同句四行。
- **[x] ① 已修复** — `_dedupedRawIndexes` 在精确键折叠后追加第二轮**邻接链折叠**：同文本 + 与链尾窗口相接/重叠（空隙 ≤ `_kChainFoldGapMs`=40ms）→ 折进链首代表行，折叠写进同一张 `repByRaw`（当前句落在链中任一段都高亮/定位到链首行）。阈值取小保证宁可漏合不错合：交替层分段空隙为 0，真实重复台词之间隔着远大于 40ms 的静默段。
- **[x] ② 已加自动化测试** — `fushi/test/media/video/video_subtitle_list_chain_fold_test.dart`：E08 形状四段相接（含同 start 特效对）折成一行；隔 2s 静默的重复「はい」不折；链与异文本行交错只折同文本链；当前句落在链中段列表不炸不空。
- **备注**：只作用于列表呈现，overlay 渲染层活动集不受影响（特效层照常全渲染，BUG-1775 管画面几何）。
