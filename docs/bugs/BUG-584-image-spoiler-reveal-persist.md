## BUG-584 · 图片防剧透遮罩点击揭开后又恢复
- **报告**：2026-07-07（用户：）· TODO-1289
- **真实性**：✅ 真 bug。根因 `hibiki/lib/src/reader/reader_pagination_scripts.dart:1432`（`_sharedInitImages` 的 `_hoshiBlurImage` 无条件给所有 `block-img` 加 `blurred` class）。揭开只在 WebView 内删 DOM class（`webview.part.dart:_hoshiRevealBlurredImage` / `reader_caret_scripts.dart` caret 揭开），未在任何跨文档存活处持久；任何章节 (重)载都重跑 `initialize()`→`_sharedInitImages` 把 `blurred` 加回。触发重载的常见路径：布局设置切换（writing mode / 分栏 / view mode / spread / prioritize / blur 开关，均经 `chrome.part.dart:_reloadWithCurrentSettings`→`_loadChapterDirectly`）、章节导航、重开书。`domStorageEnabled:false`（`webview.part.dart:1189`）故 JS 侧 localStorage 不可用。
- **[x] ① 已修复** — 揭开状态真相源改为 Dart 侧会话内存集 `_ReaderHibikiPageState._revealedImageKeys`（随本书阅读会话存活，覆盖章节重载/设置切换）。JS 两条揭开路径（点击 `webview.part.dart` + 焦点/手柄 `reader_caret_scripts.dart`）经 `window.__hoshiImageRevealKey` 算稳定 key（`<img>` src / `<svg><image>` href 相对 baseURI 解析成绝对 URL）并 `callHandler('onImageRevealed', key)` 回传；`onImageRevealed` handler 收进集合。重建章节 HTML 时 `revealedKeysJson: jsonEncode(_revealedImageKeys.toList())` 嵌回分页脚本，`_hoshiBlurImage` 命中已揭开 key 即跳过重新遮罩。提交见本轮 commit（TODO-1289）。
- **[x] ② 已加自动化测试** — `hibiki/test/reader/todo1289_image_reveal_persist_guard_test.dart`：锁住①分页/连续脚本在 blurImages=true 时消费 revealedKeysJson 且 `_hoshiBlurImage` 跳过命中项、②`window.__hoshiImageRevealKey` 暴露、③点击/焦点两条揭开路径都回传 `onImageRevealed` 且会话集嵌回脚本、④blurImages=false 边界零注入。
- **备注**：2026-09-09 的 BUG-2383 已补齐 VN：三种阅读 shell 共用同一份稳定 reveal key / 会话揭开语义，VN 在逐屏克隆完成后给当前屏大图真实加遮罩；点击或焦点揭开同时登记 JS 活集（同章返回该屏不重遮）与 Dart 会话集（重载/跨章不重遮）。跨 app 重启的持久仍不在本次范围。真机验收：开启图片防剧透 → 点击揭开某插画 → VN 前后翻屏再回来、切写排/视图或翻到下一章再回来 → 该图应保持揭开。
