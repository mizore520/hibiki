## BUG-1509 · Jimaku 字幕框偏小且搜索首帧卡顿
- **报告**：2026-08-11（用户：Wight）
- **真实性**：✅ 真 bug。`jimaku_subtitle_dialog.dart` 把桌面对话框硬限制为 720dp、左栏仅 252dp；`_search` 又在设置 `_searching` 之前先等待 API key 持久化，慢磁盘/数据库下按钮与 loading 首帧无法及时绘制，表现为点击后 UI 卡一下。
- **[x] ① 已修复** — `f5945cfacc` 先解决搜索首帧；用户实测反馈固定 1040dp 在 2K 屏仍不够宽后，`3128995d5e` 改为按视口自适应（桌面 90%、中屏 94%、手机安全边距），左栏按正文宽度在 300–420dp 间伸缩。
- **[x] ② 已加自动化测试** — `jimaku_two_pane_layout_test.dart` 锁定 360/800/1280/2048 四档视口宽度映射、左栏伸缩上下限，并静态锁定 `_searching → endOfFrame → 持久化 → client` 的执行顺序。按用户先前要求未运行自动化测试。
- **备注**：响应式版本 Windows Debug 构建成功并已启动；自动化测试按用户要求未运行。
