## BUG-2128 · 真机:KiriKiri(Limelight)游戏内单击文字推进对白而非弹查词卡,悬停高亮正常
- **报告**：2026-09-03(代理真机验证 galgame 通用性适配时发现,分支 codex/gal-generic-lookup-cards,已合并上游 develop 的构建)
- **真实性**：✅ 真机可复现(单次观测,单一 KiriKiri 样本)。原始路径:`launchoff D:\Limelight Lemonade Jam\...\limelight_lj.exe`(转区 off)→ 恢复存档进对白 → 会话 `attached=activeNative`、`nativeInputAllowed=true request=5 applied=5`、文本 hook 抓到 7 行台词、`audio=gameResource` 就绪、窗口自动绑定。WGC 抓游戏窗口真实像素:光标悬停在字形上时游戏内**出现青色高亮框**(如「そっか」的「っ」),证明注入侧 TJS 命中传感器在工作。但对当前对白行(「はい。もちろん」)**单次左键点在字形上**,游戏**推进到下一句**(【衛哉】「そっか。なら……任せるかな」),查词卡未出现,`hibiki_glookup.log` 只有重复的 `geometryAdmission ... applied=5 -> ok`,**没有 `onGalLookupHit` / `handleHit`**——即注入侧未把这次点击发布成 lookup hit,点击落给了 KAG。配置正确:`gal_hook_ingame_lookup_enabled=true`、`galHookClickLookupDefault=true`、`galHookLookupTriggerDefault=0`(左键)。
- **路径归属(关键)**：KiriKiri 的游戏内单击查词走**注入侧 in-render-tree 原生卡**(kind=1/id=1,v15 KiriKiri Layer 路由,`attached_overlayability.h` `HasInProcessRenderTreePresenter`),与本会话改的 attached 校准表面/Shift 悬浮/制卡链**不是同一条路径**。`native/galgame_hook/engine-support.yaml` 的 `kirikiri_z` `ingame_lookup_geometry` 本就是 `implemented_unverified`,本条只是补上"真机点击不弹卡"的运行期证据,与该状态一致。
- **[ ] ① 未修复** — 属注入侧 TJS/hook + 宿主 galCard 路由(v15 Layer),本会话按用户边界未读改 hook 代码。下一步定位候选(需真机):① 确认注入进程里 KiriKiri 查词 TJS 传感器的 click-submit 是否安装(悬停高亮装上了,submit 可能没装或未 swallow);② galCard 的 `overlaySize → captureReady` 握手是否完成(参考 BUG-2019 的 SGRE READY-SAFETY 抢跑,KiriKiri 用 in-render-tree 路由是否有同类竞态);③ 逐 exe 风险授权对 in-render-tree 路径是否也是门(与 attached 的 `unsafeLeftClickAccepted` 是两套)。每轮只修原始路径第一个未通过边界。
- **[ ] ② 未加自动化测试** — 待根因落定后在最强可落地层加(注入侧 submit 安装守卫 / 宿主 hit 发布契约)。
- **备注**：环境注意——本机四台显示器仅一台在线(S2719DGF OK,其余 Unknown),游戏窗口若开在断电显示器坐标区则 WGC/DXGI 抓不到、点击落在不可见处,需先 `MoveWindow` 挪到在线屏。真机驱动见 `fushi/integration_test/gal_realgame_driver_itest.dart`。SGRE 只认物理 DirectInput,单击查词验证必须真人操作。本条与 BUG-2028(KRKR 风险确认入口)、BUG-2021(KRKR 原生输入瞬态)不同:那两条是 attached/风险门,本条是 in-render-tree 点击未发布 hit。
