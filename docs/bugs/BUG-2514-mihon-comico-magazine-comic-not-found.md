## BUG-2514 · コミコ源章节阶段报 Not Found
- **报告**：2026-09-13（用户贴诊断：`stage: chapters / source: コミコ / MihonRuntimeException(BRIDGE_HTTP_500): Not Found`，栈顶 `q.chapterListParse`）
- **真实性**：✅ 真 bug，根因在 **keiyoushi 扩展侧**（`ja/comico` v1.4.2 = 最新版），本仓桥没有改坏请求；用户拍板**在本仓修、不去上游**。
  - 抛出点：`extensions-source/src/ja/comico/.../Comico.kt:119-125` `parseData()`：HTTP 200 但 JSON `result.code != 200` 时 `throw Exception(status(code))`，404 → "Not Found"；`chapterListRequest` = `GET api.comico.jp` + `manga.url` + `/episode`（:76），而 `Dto.kt:65` **无视作品 `type` 字段硬编码** `url = "/comic/$id"`。
  - 真实请求复现（经代理、按扩展方式带 `X-comico-*` 签名头）：trending 榜里混有 `type: magazine_comic`（KADOKAWA 分冊版等）的作品；`GET /comic/209156/episode` → `result.code=404 "コンテンツが見つかりません。"`（与用户报错逐字对应），`GET /magazine_comic/209156/episode` → 200、64 章。地区/登录/请求头全排除：缺签名头是 `code=400`、`Accept-Language` 非 ja 是 `code=500`，都不是 404；匿名非日本出口也能 200。
  - 正文：`magazine_comic` 章节没有 `chapter.images`，只有 `chapter.epub`。**不是加密 epub**——`epub.url` 整包在 CDN 上 `NoSuchKey`、`decryptKey` 网页阅读器根本不用；网页走 `epub.chapterEpubIncludedFile`：CDN 上**预解压**的 EPUB（`item/standard.opf` + `xhtml/*.xhtml` + `image/*.jpg` 明文 JPEG 1440×2048），只有根 URL 字符串用扩展本来就有的静态 AES key（AES-256-CBC、零 IV、PKCS7）加了密，解出来带 `#<epochMs>` 尾巴要切掉；图片请求只认 URL 上的 CloudFront 签名（`Policy/Signature/Key-Pair-Id`，约 1 小时有效），不看头。付费未解锁章 `product` 200 但无 `epub` 字段。
  - 本仓桥核对：`third_party/m_extension_server/upstream_src/.../MihonInvoker.kt:268-286,472-474` manga.url 原样；overlay `NetworkHelper.kt` 只加代理策略/日志/Cloudflare 拦截器；Dart `mihon_bridge_runtime.dart:153-171` / `mihon_models.dart:166-176` 保留原 url。
- **[x] ① 已修复** — 落在 Dart 适配器层、五端共用、不改扩展不改桥：`fushi/lib/src/media/manga/mihon/quirks/comico_magazine_comic_quirk.dart`（签名头 / AES 解密 / `magazine_comic` 章节列表 / 预解压 EPUB 按 OPF spine 出页表（xhtml 项优先走 `fallback` 直指图片，无 fallback 才拉 xhtml 取 `<img src>`/`<image xlink:href>`）/ 取图）。`MihonLibraryAdapter._chapters`：扩展在コミコ源报 Not Found 时换 quirk 拉章节，章的 `raw` 打 `fushiQuirk=comico_magazine_comic` 标记（随 chaptersJson 落库）；`resolveChapterPages` 遇标记章走 quirk 出新的 `HttpMangaPageRef`（裸 https + Referer，不经扩展 OkHttp）；`fetchChapterPage` 处理它。顺带：`OnlineMangaChapter.isLockedChapterName` 认结尾的 🔒（`Comico.LOCK = " 🔒"` 拼在结尾），コミコ的普通 comic 锁章从此也能触发登录引导而不是「You are not authorized」。`pointycastle` 从传递依赖显式声明（lock 零漂移）。**真网端到端验证**（2026-09-13 一次性探针）：作品 209156 → 64 话（最新章 🔒、最旧 `/magazine_comic/209156/chapter/19/product`）→ 29 页 → 第 3 页 302337 字节 `ffd8ffe1` 原图 JPEG；付费章 `ComicoQuirkException`。
- **[x] ② 已加自动化测试** — `fushi/test/media/manga/mihon/comico_magazine_comic_quirk_test.dart`（真密文解密、check-sum 与 Python 对拍、章节形态、OPF spine 序/fallback/linear=no/xhtml 分支、付费章异常、取图头与状态码）+ `fushi/test/media/manga/library/mihon_adapter_comico_quirk_test.dart`（Not Found → quirk 章带标记 → `HttpMangaPageRef` → 字节；非 Not Found / 非コミコ源原样抛不碰 quirk；扩展正常返回不打标记；结尾 🔒 判 locked）+ `manga_locked_chapter_and_web_env_test.dart` 更新锁名断言。
- **审查返工**：① quirk 章的锁章弹窗原本给「登录」按钮——quirk 匿名请求、登录了也拿不到付费正文，是假承诺；`OnlineMangaLoginCapable` 加按章判据 `loginTargetForChapter`，quirk 章不给登录按钮、提示换成 `manga_chapter_locked_login_unsupported_hint`（AppBar 按源的登录按钮照常）。② quirk 自己也失败（普通 comic 被下架）时原本吞掉扩展原始错误，现在抛回扩展那份（`cause` 保留原生堆栈）、quirk 错误只记日志。③ OPF 项缺 `href` 不再 `!` 崩，跳过。
- **清理条件**：上游扩展若修好 `magazine_comic` 路由，章 key 会从 `/magazine_comic/…` 变回 `/comic/…`——已下载章目录按旧 key 命名会全部变「未下载」，届时要做一次 key 迁移再删 quirk。
- **备注**：真网探针跑的就是生产 `apiHeaders()` / `pageUrls()` / `fetchImage()`（UA `fushi/manga-comico`、`Accept-Language: ja`），只把 http 客户端换成走本机代理的 `IOClient`。`BRIDGE_HTTP_500` 只是桥把扩展抛的普通 `Exception` 包成 500 的外观，"Not Found" 已是扩展原话。页表不落库（签名 1 小时过期），每次进章现拉。不追加 `m2Parameter.optimize`（CDN 重编码小图），OCR 用原图。探针脚本 `C:\Users\wrds\.claude\jobs\4019be4a\tmp\comico\`（不入库）。
