## BUG-2464 · 在线漫画改为强制下载后才能看，删除在线直读与阅读期页图缓存
- **报告**：2026-09-12（用户：产品决策，见 `docs/specs/2026-09-12-manga-download-first-design.md` §1，本条是阶段 B2 的落地记录）
- **真实性**：产品决策（spec 2026-09-12），非 bug。落地面（基于 develop `05d3cad345`）：
  - **目录与判据**：新建 `fushi/lib/src/media/manga/library/manga_chapter_storage.dart`——`mangaChapterDigest`（`sha256(chapterKey)[:24]`，与旧 `chapterDirectory()` 同算法）、`mangaChapterDirectory` = `<bookDir>/chapters/<digest>`、`isChapterDownloaded`（manga.json 存在 + pages 非空 + 每页文件经 `MangaStorage.resolvePageFilePath` 都在，**判据只写这一处**）、`deleteChapterDownload`（进 per-path 写锁）。`OnlineMangaLibraryService.add()` 直接落 `<fushi_books>/<bookKey>`；`ensureBookDirectory(row)` 把旧 `<runtimeRoot>/library/<bookKey>` 整目录搬进标准书根（跨卷 copy+delete）、改 `extractDir`、顺手清旧 `reader-cache/`；`chapterDirectory()` / `openChapter()` 删除。
  - **适配器两段式**（§3）：`OnlineMangaRuntimeAdapter.openChapter` 删除，改 `resolveChapterPages(entry, chapter) → List<OnlineMangaPageRef>` + `fetchChapterPage(ref) → Uint8List`；`OnlineMangaPageRef` 密封（`MihonMangaPageRef` / `AidokuMangaPageRef` / `InterconnectMangaPageRef`）。Mihon 取图经 `CancellableMihonRuntime.fetchImageRequest`（退回 `fetchImage`）；Aidoku 取图迁成纯函数 `fetchAidokuImagePage`（UA / Referer / cookie jar / 100 MiB 上限原样，`aidoku/aidoku_image_page.dart`）；互联走 `backend.fetchRemoteMangaPage`。
  - **删除**：`mihon/mihon_reader_chapter.dart`、`interconnect/interconnect_reader_chapter.dart`、`mihon/mihon_online_reader_page.dart`（死代码）、`aidoku/aidoku_reader_chapter.dart`（`AidokuImagePage` 等迁到 `aidoku_image_page.dart`）、`manga_page_provider.dart` 里 `MihonMangaPageProvider` / `MihonMangaReaderSession` / `_MangaMemoryEntry` / `_AsyncPermitPool` / `mihonPageCacheIdentity` / 三个缓存常量、`MangaReaderSession.prefetchAround`、`aidoku_source_browse_page.dart` 的 `_AidokuChapterReaderPage`（死代码）。
  - **下载服务** `fushi/lib/src/media/manga/download/manga_download_service.dart`（挂 `AppModel.mangaDownloadService`，HomePage 就绪后 `startMangaDownloads()` 复位 running + 起 worker；`quiesceBackgroundDatabaseWriters` 两处销毁）：单 worker 串行 `claimNextQueuedMangaDownloadJob`，任务内 4 并发取页 → `images/page-NNNNNN.<ext>`（`.tmp` → rename）→ 逐页 `pages_done` → 全齐后用 `mangaImageDimensions` 真实尺寸经 `writeMangaJsonAtomically` 写章 `manga.json` → `done`；失败 `attempt_count < 3` 退避 2s/8s 重跑（续跑认磁盘已落地的页），否则 `failed` + `last_error`；取消真中止 + 删半成品；`dispose()` 与用户取消分开（前者行保持 running 留给下次续跑）。完成钩子 `auto_ocr` → `download/manga_download_auto_ocr.dart`：与向导共用新抽出的 `manga_ocr_engine_probe.dart`（`probeMangaOcrEngines` / `resolveBackgroundMangaOcrEngine`，向导 `_refreshEngines` 已改为调它），Lens / 无可用引擎跳过并记日志，任务经新增的 `MangaOcrJobRegistry.enqueue`（按 bookKey FIFO，同书上一章还在识别时排后面，不再被 `start` 静默吞掉）。
  - **阅读器** `reader/manga_fushi_page.dart`：装载路径只剩 `_loadLocalPayload`（本地卷与已下载在线章共用）；`_openShelfChapter` 先 `isChapterDownloaded`，未下载 → `_chapterNotDownloaded` 态（key `manga_chapter_not_downloaded`，「下载」`manga_reader_enqueue_download` / 「章节」`manga_reader_pick_chapter` 两个出口，返回键照常在，订阅下载表下完自动装载）；`_switchToChapter` 目标章未下载 → 入队 + toast 停在当前章。删：`onlineChapter` 构造参数、Cloudflare 挑战字段与 UI、`_loadOnlineChapter` / `_onlineChapterIdentityMatches` / `_invalidateOnlineChapterPayload` / `_writeOnlineChapterIdentity` / `_primeOnlinePages` / `_synchronizeOnlinePageGeometry` / `_persistOnlinePayloadGeometry` + debounce / `_resolveOnlineCurrentPageFile`、拦截器的在线取图与几何回写分支。`manga_chapter_states` 双写与 `int initialPage = 0` 不变式保留。
  - **作品页 / 章节列表**（最小可用）：`MangaChapterList` 加 `downloadedChapterKeys` / `jobsByChapterKey` 状态位（未下载 / 排队 / 下载中 done/total / 已下载 / 失败，key `manga_chapter_download_<state>`）与溢出菜单「下载 / 删除下载 / 重试」；`MangaSeriesPage._openChapterAt` 已下载 → 开读，否则（未入库先 `add`）`enqueueChapter` + toast；`watchJobs` + 磁盘判据刷新状态位。
  - i18n 新增 9 个 `manga_chapter_*` key（`i18n_sync --add`）。
- **[x] ① 已按产品决策落地** — 分支 `manga-download-first`（提交见 git log `feat(manga): 在线漫画强制下载后才能看（阶段 B2）`）。
- **[x] ② 已加自动化测试** — `fushi/test/media/manga/manga_chapter_storage_test.dart`（digest 与旧算法对拍、判据四种失败形态、穿越、删除）；`fushi/test/media/manga/manga_download_service_test.dart`（假适配器真 PNG：入队→跑完→三图 + manga.json 真实尺寸 + done；单页失败 3 次 → failed + last_error + 退避表；第 2 次成功；retry；取消 running 删半成品；queued 取消 + 重入队幂等；重启复位 running；同章幂等 + 目录删后重下；autoOcr 起注册表任务 / Lens 偏好跳过）；`fushi/test/pages/manga_fushi_page_online_chapter_test.dart`（在线条目 + 已下载章 → `manga_content_ready` 且不打源；未下载 → 未下载态 + 返回键 + 点「下载」入表 queued）；`fushi/test/media/manga/manga_series_page_download_first_test.dart`（未下载章点击 → queued 行、不开读、状态位变排队；已下载章点击 → 开读、不入队）；`online_manga_library_test.dart` 加 `ensureBookDirectory` 迁移用例；`interconnect_manga_source_test.dart` 改成两段式契约；`manga_json_writeback_test.dart` 锁覆盖守卫改扫下载服务 + `deleteChapterDownload`；`manga_stats_dwell_guard_test.dart` / `manga_toggle_chrome_test.dart` / `online_manga_chapter_switch_guard_test.dart` 按新装载路径改锚点。
- **备注**：与设计稿 §4 的差异——自动 OCR 的 `imageDirPath` 取**章目录**而不是 `images/`（执行器按目录递归枚举并以相对路径写 url，从章目录起算得到的才是与章 `manga.json` 逐字一致的 `images/page-…`；已入库本地卷的整卷 OCR 走的就是这个约定）。`MangaOcrWizardEngines.resolve` 仍要 `BuildContext`（Riverpod 容器），钩子用全局 `navigatorKey` 的 context 装配、拿不到就跳过记日志。取消粒度是页：在飞的取页请求跑完才退出（Mihon 侧未接 `cancelImageRequests`），半成品目录照删。「下载全部」「完成后自动识别」chip、订阅位 v3、探针入队、mokuro 队列并表、下载中心分区留阶段 C。
