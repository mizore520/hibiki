## BUG-2541 · Magpie 超分导致校准采集与已校准内嵌查词失效

### 2026-09-19 第二批实施（原工作区，待集中验收）

用户已授权实施，下面的“只记录”段落为历史阶段。本机有界日志证据包括 `wgc_item_create_failed`（源窗口无法创建 WGC item）及 `clientMappingUnavailable`；文本 Hook 正常不能证明截图/坐标正常。

- 原表面只读 `Magpie.SrcHWND`，却使用整个输出客户区作为排版空间，忽略实际 DestRect 与黑边。现读取官方公开的 Src*/Dest* 八个屏幕物理坐标，表面按真实 DestRect 放置。仅支持完整源客户区的缩放；主动裁源、属性不全和无法证明映射时明确拒绝。
- 源 WGC item 创建失败时，捕获后端可尝试与原源 HWND 绑定的 Magpie 输出，严格裁取 DestRect，并在帧前后核对源/输出身份、客户区和视口不变。元数据保留实际被 WGC 捕获的 HWND/PID，另列逻辑源身份，不改写成源截图。
- Dart 采集与草稿存取支持这类完整视口，并以 PNG 实际尺寸作为拟合参考；不把带黑边输出客户区尺寸当作 PNG 尺寸。
- 原窗口 WGC 与 profile 恢复路径继续保留。超分实机开关、鼠标命中和截图是否受输出光标影响仍需用户一次集中验收，不能以对象编译和模拟数据测试宣称已恢复。

本批验证与最终候选见 [当前交接](../personal/GAL_LOOKUP_HANDOFF.md)。

### 2026-09-19 最新用户反馈（只记录）

- 开启超分后，无法采集当前游戏画面，之前已经校准可用的内嵌查词也失效。
- **本次退出超分后可以恢复使用**。与早期“退出后仍不恢复”的反馈分开保留；当前仍未解决的是超分开启期间的采集与查词，不把旧的持续失效状态套用到本次。
- 附件 `codex-clipboard-a1dffdfa-2ee2-401e-954d-ec3e0936cc9d.png` 显示提示：“未能取得完整的游戏画面。请确认游戏窗口未最小化，再采集一次。”页面显示 OCR 模型已就绪，现有样本为 `2240 × 1260`，保留橙框与字格。这不证明本次新截图采集成功，也不能由提示断定游戏实际最小化。
- 本次未补充超分工具版本、模式、Hook 文本状态或运行日志，暂不推断根因。沿用集中反馈阶段，只更新记录，不调查源码、不修改程序、不测试或编译。原始图片仅保留为对话附件，不提交素材。


### 2026-09-19 实施更新（待实机验收）

用户已授权统一处理。静态复核现有 `SyncToTarget → RefreshTargetClient → RefreshPresentationWindow` 会根据 `Magpie.SrcHWND` 重解析呈现目标；本批没有足够运行证据证明还需改变身份绑定。提交 `e8a9036981` 补齐有界失败诊断：失败时也保留 native reason、目标/捕获 HWND 与 PID、client/image/WGC content/texture 尺寸、surface/shield/session，经 MethodChannel 进入 `%TEMP%/hibiki_glookup.log`；不记录对白或私有路径。

截图通道 14、校准捕获 27、Magpie 守卫 7、客户区守卫 3、控制器 19 项定向测试通过。它们证明元数据及恢复契约，不证明用户超分开关路径已恢复；本条仍需同一次候选集中验收，失败日志用于区分捕获、目标身份和布局恢复。

以下为当时的收集记录。

- **报告**：2026-09-18（用户：开启 Magpie 超分后无法采集当前图像，已校准的内嵌查词也失效）
- **真实性**：待复现。用户提供了实机截图和连续操作现象；当前没有该次运行的采集 metadata、窗口句柄/客户区尺寸或运行日志，不能把根因归到截图、profile 选择或 Hook 生命周期中的某一个。
- **[ ] ① 未修复** — 用户明确要求本轮只记录，之后统一处理；本轮没有修改生产代码。
- **[ ] ② 未加自动化测试** — 需要受控比较 Magpie 开启、关闭前后同一游戏窗口的捕获身份和布局变体后再设计回归测试。

### 用户现场

- 在未开启 Magpie 前，游戏已经完成校准，文本 Hook 和内嵌查词正常。
- 开启 Magpie 超分后，样本校准页无法采集当前图像。截图中的提示是“当前没有可采集的台词。请确认游戏已附着，且正文文本正在正常获取”，但画面上正文仍可见，用户也确认文本一直能正常获取。
- 在超分期间，之前已经存在的内嵌查词也失效；关闭 Magpie 后仍没有恢复。用户没有报告文本 Hook 同时中断。
- 截图显示样本页仍能显示游戏画面和已保存的字格/橙色区域；截图标注的样本尺寸为 `1400 × 1050`。截图只作为本次对话附件保留，没有复制进仓库。

### 当前边界

现有校准契约本来就会拒绝 Magpie 重定向、客户区尺寸不符或缺少完整 metadata 的截图。因此“不能采集”可能是保护性拒绝；但关闭超分后仍不能采集、已校准查词也没有恢复，说明还需要单独调查状态恢复和目标身份变化，不能简单归为“超分不支持”。

后续统一处理时需要对比：

1. Magpie 开启/关闭前后的目标 HWND、PID、客户区物理尺寸、DPI 和捕获 metadata；
2. WGC 捕获是否仍指向游戏客户区，还是变成 Magpie 输出窗口/重定向表面；
3. 现有 profile 的 reference client 与 surface variant 选择是否被尺寸变化拒绝；
4. 关闭 Magpie 后是否重新绑定目标、清理旧 capture lease、恢复 overlay 和已校准查词，而不要求用户重新 Hook。

本条记录不证明 Hook 文本获取失败，也不证明 Magpie 对所有游戏都不兼容；本轮不修复、不编译、不要求用户重复测试。
