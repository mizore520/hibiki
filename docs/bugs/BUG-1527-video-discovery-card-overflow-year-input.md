## BUG-1527 · 视频发现卡片溢出且年份下拉过长
- **报告**：2026-08-11（用户：Wight）
- **真实性**：✅ 真 bug。`video_discovery_page.dart` 的推荐横栏高度固定 224、海报网格比例固定 0.52，在当前字体/UI 缩放下卡片内容高出约 2.4px；年份筛选则每次生成 1900 至今的一百多个 `PopupMenuItem`。
- **[x] ① 已修复** — 本提交将横栏增高到 240、网格比例放宽到 0.50；年份改为四位数字输入，350ms 防抖自动筛选并提供清空按钮。
- **[x] ② 已加自动化测试** — 更新 `video_discovery_page_test.dart`，守卫年份输入发出 1999 请求及空值/不完整/越界年份解析。按用户先前要求未运行自动化测试。
- **备注**：用户要求改完启动查看，暂不更新 PR。
