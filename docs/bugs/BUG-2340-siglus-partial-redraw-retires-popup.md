## BUG-2340 · Siglus同句分批重绘错误撤销已打开词典
- **报告**：2026-09-07（扩大官方 Siglus 原版体验版覆盖时发现）
- **真实性**：✅ 真 bug。`native/galgame_hook/hook/adapters/siglus_lookup.inc:1387` 要求最新 glyph 尾部构成完整正文，同句重绘跨 worker tick 时临时将 `active_valid` 清零；`ProcessSiglusLookupTick` 据此 Retire 整个 provider。宿主 `gal_ingame_lookup_controller.dart:564` 在撤销准入后立即销毁词典。完整 ABC 后只到达下一帧 A，即使正文和坐标不变也发生退役；随后 BC 恢复 Ready。临时不可点击被错误解释为引擎能力失效。
- **[x] ① 已修复** — 已拆分同正文已证明布局的 provider 生命周期与当前批次可点击性；不延时、不复用不完整批次的旧坐标。新正文、profile、viewport、窗口或会话失效仍退役；独立 snapshot_epoch 禁止分批重绘前的按下事件在恢复后提交。重新从原版 Start.exe 进入 PID 36724、宿主 24584/helper 31648，以 DLL `6f18f98cb239c7825cf3ec3c68a0b1af75d86c38ffd18cf28c672347d8a4a86c` 默认附着：单次点击成功查词，停留超过一分钟未自动撤销；两次不同原文点击均显示新明解，窗外关闭保持原句，下一次普通点击才推进。
- **[x] ② 已加自动化测试** — `native/galgame_hook/tests/siglus_lookup_test.cpp` 使用生产布局状态机覆盖跨 tick 同句重绘、首批不完整、新正文、布局变化和在途释放；x86/x64 完整构建与 CTest 各 72/72 退出 0。`adapter_structure_test.py` 保留严格最新 glyph 尾门并验证健康检查与 provider 接线，41/41 通过。未因此宣称全部字体/排版、音频配对或制卡 E2E。
- **备注**：原版終のステラ体验版 x86 1.1.137.0，EXE SHA-256 `b3d0bc77fd043c93e5ea469cf05341a541de425ea76afc295c5f5d85984526c5`。最终 owner v24 DLL `eef92ede7c8c10cb88ad7a68e67784c9ad335f729713def14b3213f517743488`，game PID 37120、helper 71740、host 58124。16:12:17.468794 已真实显示新明解词典；16:12:27.918473 nativeInputAllowed=false，16:12:27.919471 宿主先主动 dismiss，16:12:27.930862 恢复 true。随后一次窗外点击推进，不能将弹窗已先退役后的点击称为已证明的 shield 漏 up。此前 LUNARiA v23 也观察到相似短暂撤销，但本条不倒推其全部原因。
