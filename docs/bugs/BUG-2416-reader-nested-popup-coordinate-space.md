## BUG-2416 · 阅读器嵌套查词混用屏幕与浮层坐标导致遮字
- **报告**：2026-09-10（用户截图：应用内竖排阅读器中再次查词，子弹窗盖住所查文字。）
- **真实性**：✅ 真 bug。`fushi/lib/src/pages/base_source_page.dart:828` 的嵌套回调使用 `popupWordScreenRect` 的全局屏幕坐标，直接交给页面内 Stack 的局部定位函数。标题栏/页面偏移及祖先缩放被重复应用；贴词上方的子弹窗底边会落入原词。正文原有选区是阅读器局部坐标，不能在布局函数统一减屏幕偏移。
- **[x] ① 已修复** — 浮层 Stack 提供目标坐标域，普通文字和词条链接的初始锚点、命中后整词重锚均经两角 `localToGlobal → globalToLocal` 转换。目标 RenderBox 不可用时保留原局部 fallback；使用全局坐标的其它宿主维持原契约。实现见本条同一提交。
- **[x] ② 已加自动化测试** — `fushi/test/pages/reader_nested_popup_coordinate_space_test.dart` 在非零页面偏移、1.0/1.5 倍祖先缩放下复现旧坐标盖词，验证新坐标不重叠、整词重锚和目标未挂载 fallback，并守卫两个回调四个接入点。连同既有定位、整词锚点测试共 61 项通过；相关文件 analyze 无问题。
- **备注**：首次测试因 PDFium 下载失败在用例前阻塞，使用本机代理完成依赖下载后重跑通过。未重新构建安装 Windows 应用，用户原书原页面实际复测待补；不把组件回归测试当作真机验收。
