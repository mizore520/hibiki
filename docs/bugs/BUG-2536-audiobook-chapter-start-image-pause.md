## BUG-2536 · 有声书章首插图既不图片等待也不揭防剧透遮罩
- **报告**：2026-09-14（用户：如果图片在章首，不会暂停并且不会撤遮罩）
- **真实性**：✅ 真 bug。沿 `_handleCueCrossChapter` → `_navigateToChapter` → `_onRestoreComplete`（`navigation.part.dart` 的 post-frame `AudiobookBridge.resetImagePauseAnchor` 把 `__fushiPrevHighlight` 归零）→ 新章第一条 cue 的 `_onCueChanged` → `AudiobookBridge.highlight` → JS `__fushiImagePauseAdvance(el)` 复现：`fushi/lib/src/media/audiobook/audiobook_bridge.dart:__fushiImageBetween` 首行 `if (!prev || ...) return null`，新文档里锚点为空（fresh doc 是 undefined、恢复完成后是 null），文档开头到第一句之间的图（章扉画 / TODO-1128 合并进宿主顶部的单图片章）从不进暂停判定，`__fushiRevealBlurredBetween(prev, el)` 同样在 `!prev` 处早退不揭遮罩。章内相邻两句之间的图不受影响（BUG-007 / TODO-1367 契约仍成立）。
- **[x] ① 已修复** — 本提交（自含修复+测试+本条目；并发抢号致哈希随 rebase 变动故不内联记录）：① reader `_handleCueCrossChapter` 在音频驱动的跨章导航前记下到达章 `_audioChapterArrivalSection`；`_consumeAudioChapterArrival` 只在目标章已落地（`!_restoreInFlight && _currentChapter == section`）且本次高亮是真实 cue（`textFragmentId` 非空——未匹配 cue 的清高亮不消费，等第一条匹配 cue）时消费一次，经 `AudiobookBridge.highlight(fromChapterStart: true)` 透传到 JS 两条高亮路径（sasayaki `__fushiHighlightSentenceAudioCueById` / selector `__fushiHighlight`）。② JS `__fushiImagePauseAdvance` 收到 `fromChapterStart` 时把 `document.body` 当作上一句锚点：body→el 区间 = 文档开头到第一句，章首图照常 `onImageDetected`（图片等待）+ `__fushiRevealBlurredBetween` 揭遮罩 + reveal 时视口落到插图。手动跳章 / 位置恢复不带标记，行为不变（章首图没被音频读到）。③ `_pauseThroughImageOnlyChapters` 改回报「最后一次停留是否已落在目标宿主页」：被吸收单图片章场景（TODO-1128）已在宿主顶部停留 + `revealAllBlurred` 过，不再二次触发。④ `__fushiImageBetween` 跳过 `gaiji` / `gaiji-line` 内联外字（与 `__fushiRevealBlurredBetween` 一致）：章首锚点把章名也纳入区间后，标题里的外字不能触发图片等待。
- **[x] ② 已加自动化测试** — `fushi/test/media/audiobook/chapter_start_image_pause_test.dart` + `chapter_start_image_pause_test.js`：node 真执行从 `audiobook_bridge.dart` 切出的 `_highlightFn` / `_sentenceAudioFn`，fake DOM（compareDocumentPosition / contains / querySelectorAll）上 8 组断言——章首图触发 `onImageDetected` + 去 `blurred` + `onImageRevealed` 持久 + 懒图强制 eager + 视口落到图、第一句之后的图不提前揭；不带标记（手动到达）不动章首图；显式归零后带标记同样以 body 为锚；章名 gaiji 不算插图；章内 s1→s2 推进不受影响；`pauseEnabled=false` 仍揭遮罩不滚图；sasayaki / selector 两条路径都透传；图片防剧透关闭时揭遮罩 no-op 不报错。变异验证：换回上游旧 JS 首条断言即红。另有 reader 接线 / Dart→JS 透传源码守卫；既有 `image_pause_detection_test.dart` 的签名锚点与扫描窗口随之更新。
- **备注**：JS 行为在 fake DOM 上验证，真 WebView 未复测（需带章首插图 + 对齐音频的书）。`__fushiHighlight('')`（未匹配 cue 清高亮）仍会把锚点归 null，因此「匹配句 → 章中未匹配句 → 匹配句」之间的图目前也检测不到，属同一家族的另一缺口，本轮未动（改清高亮语义影响面更大，另开）。
