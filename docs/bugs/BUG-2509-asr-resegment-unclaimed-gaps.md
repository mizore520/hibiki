## BUG-2509 · 转录字幕对齐后 cue 边界落在词中（艦｜長、停｜船）：正文没被认领的缝让重切串断开
- **报告**：2026-09-13（用户：群里 tester 截图，有声书 cue 视图 `艦` / `長は…命じた。` / `停` / `船せよ。` 各成一条；「字符数没少、匹配率高，但字的读音对不上轴」）
- **真实性**：✅ 真 bug。上游 `fushi-subtitles` `align` 命令 + 本机 kagami 素材（`D:\APP\asr_bench\kagami\ours_v1.srt` + `.tokens.jsonl` + `kagami.epub`）复现：对齐产物 32 处词中/句中边界（`浮か｜私に`、`気にし出｜もう`、`こと｜心｜は、`、`履い｜そ｜う｜足の指`、`似顔絵と｜嶋」`）。根因 `packages/fushi_audio/lib/src/matching/cue_sentence_resegmenter.dart:153`（改前）`_contiguous` 要求相邻命中区间**严格首尾相接**，而匹配器只把「听到的字」映射到正文——ASR 整句掉字（4 s 的 cue 只吐了一个「こ」，缝 11 字）、模糊命中的词尾（`気にし出｜す。`，缝 3 字）、书写假名而听写成汉字（`こころ` ↔ `心`，缝 4 字）——正文里没被任何 cue 认领的字成了缝，缝一出现串就断，两侧的词中边界永远合不回去。三阶段探针（core / gap-fill / resegment）逐条证实；`AnchorGapFiller` 按文本长度比 [0.3, …] 拒掉这些缝（1 字 vs 4 字、2 字 vs 31 字）。匹配率报 96.8% 是真的，边界是假的——率不衡量边界。
- **[x] ① 已修复** — 重切建串前加合缝 `_closeGaps`：缝归缝隙期间正在发声的 cue（中间有未命中 cue 按发声时长比例分给它们，0 字缝 = 幻觉认领空区间后折进前一片；没有就在缝内第一个标点边界切开分给两侧），判据是朗读速度（认领后密度 ≤ 命中 cue 密度中位数 × 2，自适应，旁白真跳段时缝保留）。合缝后现有的词中边界抹掉 / 句末标点新增逻辑原样收掉全部病例。上游 [fushi-subtitles#17](https://github.com/hajisensai/fushi-subtitles/pull/17)（`63bf038`）与本仓 `fushi_audio` 拷贝同一份 diff。kagami：词中边界 32 → 14（余下为书名页与逐行歌词），多句 cue 19 → 0。
- **[x] ② 已加自动化测试** — `packages/fushi_audio/test/matching/cue_sentence_resegmenter_test.dart` 「合缝」组 5 条（假名 vs 汉字单字 cue / 整句掉字按时长分缝 / 词尾缝按标点切开 / 幻觉 0 字缝 / 跳段超速不合缝）；真实数据对照 `fushi/test/asr/realdata/asr_realdata_match_test.dart`（`ASR_REALDATA_*` 环境变量指向 kagami）打印 `gapsClosed=41`。上游 `asr_align` 同 5 条。
- **备注**：`CueResegmentStats.gapsClosed` 单列进日志，不混进命中率。歌词视图的显示本身是另一件事（同轮第二步）。
