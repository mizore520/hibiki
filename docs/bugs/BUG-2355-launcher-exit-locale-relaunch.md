## BUG-2355 · 启动器正常退出被误判转区崩溃并重复启动
- **报告**：2026-09-08（Windows 原始 Start → StartMenu 启动链，helper 等待超时后出现重复启动提示）
- **真实性**：✅ 真 bug。`fushi/lib/src/mining/gal_hook_session_controller.dart:1608` 将 `japaneseLocaleApplied + launchedPid 已退出` 直接认作转区游戏崩溃；Start 正常退出但菜单仍活着也满足条件。`fushi/lib/src/mining/galgame_audio_source.dart:200` 原解析器用 `firstMatch`，即使 helper 已回报真实子进程，仍保留首个启动器 PID。两者使 auto 档递归用 off 再开一次原入口。
- **[x] ① 已修复** — 本提交增加 helper LAUNCH 的角色和实际 locale 启动结果字段（`native/galgame_hook/injector/injector_main.cpp:3003,3076`）；宿主只采用最后一条完整记录，只有已明确 game 角色且 locale 启动成功的目标才可进入既有退出回退。launcher、旧协议角色未知、native 已回退普通 CreateProcess 都不能授权自动二次启动。显式 on 与存活 game 不回退；递归保留 contentLanguage。不改用户持久化偏好、不增加延时/重试。
- **[x] ② 已加自动化测试** — `fushi/test/mining/gal_locale_launch_fallback_test.dart` 实际 controller 行为覆盖启动器退出/菜单存活、真实子进程更新、直接游戏退出回退、on、不转区/未知角色；`fushi/test/mining/galgame_audio_test.dart` 覆盖每个 stdout 切分点、真实 source stdout drain 获取最终 PID/角色及 native 两个输出位置的接线守卫。两文件最终共 **86/86，exit 0**；4 个改动 Dart 文件 focused analyze **exit 0，无问题**。未重复计入较早的 85 个测试运行。
- **备注**：本轮只验证源码/定向测试；Windows 双架构 helper 构建及原始启动菜单真机复测由集成任务完成，尚未宣称运行时问题全解决。helper 的完整 child discovery 预算与宿主总 readyTimeout 都是 30 秒，等待菜单后续操作仍可能超时，未在此补丁延长或盲重启。游戏是否退出仍沿用现有目标探测合同；此补丁只补角色/来源证据，不宣称取得异常退出码。工作台同路径命中库条目时已传 mode/language；实际某次首 launch 没带 locale 的原因尚未确认，未当作此修复结果。私有日志：`.codex-test/siglus-engine-adapter/launch-role-tests-final.log`、`launch-role-analyze.log`，不入库。
