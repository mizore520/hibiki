## BUG-2391 · Windows视频退出全屏后画面停滞需拖动窗口恢复
- **报告**：2026-09-09，用户退出 Windows 视频全屏后画面停滞，拖动窗口即恢复。安装版 2.3.0-debug.14174。
- **真实性**：用户现场确认；主窗口 WM_NULL 正常响应，错误日志没有对应异常。当前未在隔离测试中复现停帧。静态定位到呈现请求缺口：`fushi/windows/runner/flutter_window.cpp:522` 的全屏通道在 `SetFullscreen` 完成后直接回复，没有显式请求 Flutter 重新呈现。全屏/最大化转换可能保持客户区尺寸；`win32_window.cpp` 的过渡快照清理只释放 bitmap，不负责提交新的 Flutter 帧。该机制与拖窗恢复一致，但不能据此排除视频纹理或驱动同步问题。
- **[x] ① 已修复呈现请求缺口，现场验收待完成** — 在共享 Windows 全屏通道完成原生几何恢复和快照清理后调用 `ForceRedraw()`，早于 channel 回复。比较切换前后真实状态，并检查控制器存活，仅实际切换请求一次；覆盖视频与 F11 的同源入口。没有定时重试或模拟窗口抖动。
- **[x] ② 已加自动化测试** — `fushi/test/build/windows_fullscreen_redraw_guard_test.dart`：去注释后验证真实状态比较、原生切换→重绘→回复顺序、一次重绘及无定时/窗口抖动。源代码守卫只证明接线，不能证明显卡呈现效果。
- **备注**：正常 media_kit 帧通知与 Dart 标题栏恢复也会请求帧，不能称为完全没有后续帧请求。此次不改变 media_kit 或 HDR 独立窗口；用户设备原始退出全屏路径与 HDR 未验证。验证结果见提交说明。
