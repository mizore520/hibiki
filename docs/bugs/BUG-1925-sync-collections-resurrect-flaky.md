## BUG-1925 · 全量下 sync_orchestrator_collections 的 peer-republish 用例偶发红（4 次全量红 2 次，单跑绿）
- **报告**：2026-08-29（发现者：BUG-1922 合入前的本地全量）
- **真实性**：✅ 真复现，但**低频**。红的是 `fushi/test/sync/sync_orchestrator_collections_test.dart` 里 `finding1 no self-resurrect on 2nd sync (file lastWrittenAt fold)` 组的 `peer republishes member later than tombstone → intentional re-add wins`，失败点是用例**中段**的 `test/sync/sync_orchestrator_collections_test.dart:269`（`expect(await b.orderOf('Fav'), ['y','z'])`，即 B 同步后应当已应用 A 的移出）：`Expected: ['y','z'] / Actual: ['x','y','z']`——x 没被移出。本机实测样本：带 BUG-1922 改动的全量 **4 次（21180 绿 / 21181 红 / 21177 红 / 21181 绿，2 绿 2 红）**，移出该 PR 唯一新增测试文件后的基线全量 1 次（21174 绿），单独跑该文件 1 次（10 tests 绿）。跑红的两次里第三次同时还有 `test/pages/video_subtitle_dialog_registry_test.dart` 的 `Bad state: Cannot close sink while adding stream`——那条是已知的宿主 IPC 崩溃型伪红，与本条不同类。红的时候本机**没有**并发 flutter/dart 进程（`Get-Process` 实测为空），所以不是「多 agent 抢 sqlite3.dll」那类并发伪红。
- **[ ] ① 未修复** — 未定位到确定根因，只有线索：裁决量 `CollectionManifest.lastWrittenAt` 与墓碑 `publishedAt` **同源**，都取自 `syncCollections` 开头的 `final int nextBaseline = DateTime.now().millisecondsSinceEpoch`（`fushi/lib/src/sync/sync_orchestrator.dart:859`），判据是严格 `>`（`fushi/lib/src/sync/collection_sync_engine.dart:90` 起的文件级折叠）。测试用 `tick()`（`test/sync/sync_orchestrator_collections_test.dart:94`）铺时序，而它只保证**墙钟前进 1 毫秒**。静态推演 `seedConverged()` 的 tick 布置时没找到必然翻转点，所以还不能断定是「测试时序太紧」还是「引擎在时戳相等/相近时裁决不稳」——**后者会是用户可见的真 bug（已删除的合集成员在下一轮同步复活）**，不能按纯测试问题结案。
- **[ ] ② 未加自动化测试** — 待根因确定后再决定加在哪层；现有用例已经能复现，缺的是稳定复现手段（建议下一步：把 `tick()` 换成可注入的假时钟，或让引擎裁决显式区分「同毫秒」，再用确定性时序把这个用例钉死）。
- **备注**：与 [BUG-1922](BUG-1922-macos-aidoku-runtime-not-bundled.md) 无因果——那条只改 CI workflow / macOS 打包脚本 / 新增一个纯读文件的守卫测试，不碰 sync、DB、时钟或文件写入；移出该守卫测试后的基线全量同样是绿的，而带该测试的全量四次里两绿两红——说明它至多改变了执行集调度概率，不是诱因。BUG-1922 因此照常合入，本条独立跟进。
