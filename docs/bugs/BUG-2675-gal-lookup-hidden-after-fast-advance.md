## BUG-2675 · 快速翻页后新台词暂时没有内嵌查词
- **报告**：2026-09-25（用户：リトルバスターズ！EX，快速点击翻到下一句时，新台词上没有高亮也不能点，要等一会才出现；已确认是快速翻页，不是在同一句上连点查词）
- **真实性**：⚠️ 根因未完全确认。native 收到新台词后会立即重排（`UpdateText` 直接调用 `SyncToTarget`），本身没有防抖。用户当次日志里有一段没有弹出查词窗口的等待：10:51:56.202 进入 `shieldHandshakePending`，10:51:58.204 才恢复，和 [BUG-2674](BUG-2674-gal-lookup-click-after-popup-ignored.md) 是同一个闸门。为什么翻页点击会让拦截重新握手，还没有在日志里单独确认。
- **[ ] ① 候选修复（根因未确认，待复测）** — `fc1d095263`：同 BUG-2674，握手等待期间改为每 16 ms 检查一次。如果翻页后的空窗确实来自这个握手，空窗会缩短到接近一帧加上游戏回应的时间。
- **[ ] ② 未加自动化测试** — 同 BUG-2674。
- **备注**：复测时如果快速翻页后仍要等，请记下大概时间；新增的 `gal-shield:` 日志会记录每次等待的时长和原因。如果那段时间没有 `gal-shield:` 记录，说明是别的闸门，需要另查。
