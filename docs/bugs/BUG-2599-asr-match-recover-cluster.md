## BUG-2599 · 有声书 ASR 字幕匹配：恢复扫描单条精确命中把游标钉到书中段、整本只命中 1.7%
- **报告**：2026-09-19（用户：『妹さえいればいい。』第 2 卷 m4b + EPUB，设备端转录后「匹配率很低」；库里 `audiobooks.health_reason` = `134/679 cues matched`，20%）
- **真实性**：✅ 真 bug。转录产物本身健康（4719 条、每条均长 17 字、全书 33 个 10 分钟桶无退化）。离线 realdata 对照（`fushi/test/asr/realdata/asr_realdata_match_test.dart`）复现：阈值 0.8 第一遍 79/4719（1.7%）、回填后 151/4719（3.2%）。起点 `startCursor=9` 是对的；坏在 `packages/fushi_audio/lib/src/matching/epub_srt_matcher.dart:329-341`（修前）的恢复扫描：连续 20 次 miss 后拿**当前这一条** cue 在 `big.indexOf(nc, cursor)` 找**第一次出现**就搬游标，无任何邻句佐证。片头「登場人物」页（EPUB 里是插图，无文本）连 miss 20 多条后，第 28 条「大野アシュリー」在正文里第一次出现是第 18 节（该角色中段才登场），游标被钉到 16009/80576；游标单调不回退，序章起第 9~17 节全部落在游标之后 miss；之后每次恢复又被「楽しそうだな」「どうしたんですか？」这类泛用短句再往后钉（35 → 39 → 54 节），只有末尾あとがき顺序对得上才密集命中。游标轨迹见库里 `audio_cues.text_fragment_id`：`s=1 → s=18 → s=35 → s=39 → s=54 → s=55`。
- **[x] ① 已修复** — `fe993e708e9`：起点检测的「聚簇佐证」抽成 `EpubSrtMatcher._clusterAnchor`，恢复扫描也走它：从当前 cue 起最多扫 72 条凑 24 条探测 cue，在全书找候选，取被最多条 cue 佐证（同一 3000 字内）的位置；至少 `recoverMinSupport=3` 条、或至少 2 条且全体一致才往前搬，佐证更多时允许回退（同分优先游标之后最近处）；恢复不做余量检查（书尾只剩两三条正文 cue、剩余音频可能整段不在书里），全书模糊扫描每次封顶 8 条；搬动后清掉尾巴回吃参照。找不到就原地不动、再攒 20 次 miss 才试下一回。同书离线对照：第一遍 67.0%、回填后 90.4%、重切后 4474/4727（94.6%）；剩余未命中为登場人物页、章间人物卡（「土岐健次郎 年齢二十六歳…」）、作品紹介广告、旁白语气词等音频独有内容。
- **[x] ② 已加自动化测试** — `fushi/test/media/audiobook/epub_srt_matcher_test.dart` 三条 `BUG-2599 恢复扫描：…`（片头人名不钉到书中段 / 章节顺序与 spine 不一致时佐证够多允许回退 / 只有一条泛用短语撞中时游标不动），旧实现上全红、新实现全绿；`packages/fushi_audio/test/matching/epub_cue_matcher_test.dart` 两条「30 条垃圾 cue」夹具照旧通过（书尾双锚点靠「全体一致」规则恢复）。
- **审查追加（2026-09-21，合并前）**：① 收敛规则从「簇内 cue 序最靠前的探测 cue 的首处命中」改成簇内 (cue 序, 位置) 的**最长递增子序列首处**（`_monotoneChainStart`）——人名 / 泛用短语落在真簇 3000 字内时，旧规则仍会把游标拽到它身上、簇里其余二十几条正文全落在游标之前（用例 1 改成 filler 200 句 / 60 句两档，60 句档旧规则红）；② 「≥2 且全体一致」要求每条在全书恰好精确出现一次（`allExactUnique`），两条泛用短语在全书某处 3000 字内共现几乎必然；③ 恢复扫描的全书模糊扫描加整次匹配级总预算 `recoverFuzzyBudgetTotal=64`——选错卷 EPUB（全书零命中）时原 PR 每 20 条 cue 跑 8 条全书 Dice，4700 条 cue 单遍实测 58 s（develop 旧实现 2.6 s）且 app 侧要跑 4 遍。同书离线对照：第一遍 67.1%、回填后 4195/4719（88.9%）、耗时 40 s（原 PR 90.4% / 87 s）——差的 71 条是原 PR 靠一条孤立撞中「好きなものH」（第 49 节）把章间人物卡（3931–4003，音频独有）按比例灌进第 48/49 节的**假阳性**；新版不填它们，反而锚住了原 PR 整段漏掉的第 48 节正文（4014–4023）。
- **备注**：用户装的是上游 debug 构建 2.8.0-debug.15166，匹配代码以 `upstream/develop` 为准；修复在 `pr/asr-match-recover` 分支。排查顺序沿用 BUG-2147：先看 startCursor → 再按时间分桶看 ASR 产物 → 最后看游标轨迹（`text_fragment_id` 的 `s=` 跳变）。已入库的这本书要**重新导入字幕**（再走一次「使用字幕」/ 导入 `transcript.srt`）才会用上新匹配器；「重新匹配」只对库里已被重切合并成 679 条的 cue 重跑，回不到 4719 条。
