## BUG-2162 · 用户库 user_version=95 却缺 v52/v57/v87/v88 台阶产物，导入书撞 epub_books 无 language 列
- **报告**：2026-09-05（用户：shishamo，Windows release 包导入书 + 转录字幕时失败）
- **真实性**：✅ 真 bug。`error_log.txt`：`SqliteException(1): table epub_books has no column named language`，栈 `epub_importer.dart:175 _persistParsed` → `database_content_misc.part.dart:139 insertEpubBook`。读用户主库 `%APPDATA%\Fushi\Fushiushi.db`：`user_version=95`，但与代码 schema 逐表对比缺 9 处——`epub_books.language` / `dictionary_metadata.language_override` / `video_books.language` / `srt_books.language` / `galgames.language`（v87/v88 台阶）、`media_collections.audio_track_id` / `subtitle_delay_ms`（v52）、`collection_member_tombstones` 与 `book_tag_membership_tombstones` 仍是 `removed_at` 未改名 `deleted_at`（v57）。根因：该库的版本号在另一条并行迁移史（本仓与上游 schema 号曾重号）上先被写高，`packages/fushi_core/lib/src/database/database.dart` 的 `if (from < 52/57/87/88)` 对它永远不再进入；v88 注释早已写明「已发布的版本号只读，加列只能往上开新台阶」。备份 `before-schema-v88-compat-20260815` 里 v88 库同样没有这些列，可佐证。
- **[x] ① 已修复** — schema v97（`FushiDatabase.schemaVersion` 合并 upstream 后落在 97；提交标题里的 v96 是合并前的台阶号）：`_replayColumnStepsForDriftedSchema` 把 v52/v57/v87/v88 的加列/改名语句幂等重放一次（`_columnExists` 守卫，正常库全部 no-op）；多余列（`video_download_jobs.episode` 等）不删。提交见 git log（`fix(db): v96 幂等重放…`）。
- **[x] ② 已加自动化测试** — `fushi/test/database/migration_v97_schema_drift_repair_test.dart`：从「当前库 DROP 七列 + 两列改回 removed_at + user_version=95」的真实漂移形状出发，断言升到 97 后九处全部补齐、墓碑值搬到 deleted_at、存量行无损、`insertEpubBook`（线上炸的那一句）能落库；另一条守正常 v95 库升级全部短路不报错。
- **备注**：漂移库里另一条史留下的多余列（`video_download_jobs.episode`、`video_metadata_images.is_primary`）无害，不动。
