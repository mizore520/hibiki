## BUG-2208 · 阅读器外观/目录/搜索等面板打开期间阅读时钟照跑
- **报告**：2026-09-06（用户：）
- **真实性**：✅ 真 bug。根因 `fushi/lib/src/pages/implementations/reader_fushi/chrome.part.dart:1662`（`_showAppearanceSheet`：外观 / 导航目录 / 书内搜索 / 收藏 / 有声书面板）、`:2221`（`_openReadingStatistics`）、`:2261`（`_openAlignmentImportDialog`）、`:964` / `:1005`（`_openImageViewer` / `_openGallery` 全页路由）、`audiobook.part.dart:1877` / `:1907`（导入 / 重导入对话框）、`lyrics.part.dart:311`（修复前行号）——所有压住正文的面板 / 弹层 / 路由都不停表，时钟只受生命周期与手动暂停控制。面板里调半小时参数全算阅读时长（审计 A3）。
- **[x] ① 已修复** — 提交：`003149107a`；对齐 Hoshi Android `modalPaused`：页面加 `_studyClockModalDepth` 计数 + `Future<T> _withStudyClockPaused<T>(body)`（进入 `depth++` → `_syncStudyClockRunState()` 停表结算落库；finally `depth--` → 按判据续表），`studyClockMayRun` 把 `modalDepth == 0` 纳入判据。接到 9 个入口：`_showAppearanceSheet`（呈现分派抽成 `_presentQuickSettings`）、`_openReadingStatistics`、`_openAlignmentImportDialog`、`_openAudioImportDialog`、`_openSrtBookReimport`、`_openImageViewer`、`_openGallery`、`_transcribeFromAudiobookPanel`、`_showLyricsModeHintIfNeeded`。仓库 `RouteObserver` 只挂在 `AppModel` 上无人订阅且只通知 `PageRoute`（对话框 / sheet 全覆盖不到），故选在调用点包一层，不引入 `RouteAware`。**不接**查词浮窗与 Anki 制卡（mining.part）——那是阅读的一部分。
- **[x] ② 已加自动化测试** — `fushi/test/reader/reader_study_clock_policy_test.dart`（modalDepth 判据）+ `fushi/test/pages/reader_study_clock_gate_guard_static_test.dart`「BUG-2208」组（`_withStudyClockPaused` 计数进出、9 个入口逐一必须经它、mining.part 必须不经它）。
- **备注**：
