## BUG-2492 · 分页首字探针兜底返章首 0 把整段前文计成已读
- **报告**：2026-09-13（用户：「今天 10:23→10:26 读了 5548 字速度异常 iOS 端的」，附 `fushi_study_diag_log.txt`；iOS 27.0 / app 2.6.0-beta.14741，無職転生 22 第 9 章，听书 + 分页阅读）
- **真实性**：✅ 真 bug。诊断日志是铁证：`10:25:41 arrive [5406,5429) next` → `10:25:46 arrive [278,5450) back -5128c`（账本随即 `-346c taken=346 session=0c`）→ `10:25:50 arrive [5450,5483) next` → `+5172c`。278 恰是该章的全书累计基址（多处 `open resume point charOffset=N` 与其后 `arrive` 起点可反推），即 JS 那次返回了**章内 start=0、end=5172**：页尾对（正是所在页 [5429,5450) 的尾），页首错成章首。该 session 5548 字 = 5172 幻象 + 376 真读（137 s，折合约 1 万字/时，与其它 session 的 1.4~1.8 万/时同量级）。根因链：`fushi/lib/src/pages/implementations/reader_fushi/webview.part.dart:1533-1544`（`fushiProgressDetails` 用 `getFirstVisibleCharOffset()` 取起点、`getLastVisibleCharOffset(off)` 取终点，两者独立探测）→ 分页 shell `getFirstVisibleCharOffset`（`fushi/lib/src/reader/reader_pagination_scripts.dart:2876`）页首角 `caretRangeFromPoint` 落在非文本（该书插图是 `<p><img class="fit"/></p>`，两页只有 23/21 字正是插图页）→ 走 `firstVisibleCharOffsetByScanPaged()` 兜底（`:1434`）→ 兜底的「在视口首边之前」判据（`:1504-1512`：竖排 `rect.left >= body.clientWidth`、横排 `rect.bottom <= 0`）抄的是**连续模式的轴**；分页是 CSS multicol 沿翻页轴排列（`getScrollContext` / `buildPaginationMetrics` 都按竖排 y / 横排 x），前页在竖排的上方 / 横排的左侧，永远不满足 → 逐节点累加恒 0 → 兜底把任何页报成章首 0。0 是合法值：`getLastVisibleCharOffset(0)`（`:2683`）照用，`end(5172) > 0` 直接返回；Dart `navigation.part.dart:1233` 只查 `unitEnd > unitStart`，5172 字的「一页」原样进 `ReadUnitLedger`——账本按契约先撤回（位置退到 0）再在下次翻页把 [0,5172) 整段入账。这个 0 以前只喂落库锚点，被 `scrollToCharOffset` 的 `charOffset <= 0 → 按 hint 保当前页` 掩住（TODO-773 真机复测看不出来）；统计接入第三/四段后同一个 0 放大成整段字数。
- **[x] ① 已修复** — 本 PR：① 分页扫描兜底改沿翻页轴（新 `pagedFirstEdge` = 竖排 padding-top / 横排 padding-left，与分页 caret 探点 `pt + 2` / `pl + 2` 同量纲；`countCharsBeforeViewportPaged` 起止边取竖排 top/bottom、横排 left/right，三态短路横竖同式；`isTextOffsetBeforeViewportPaged` 判 `(vertical ? rect.bottom : rect.right) <= firstEdge`）；② `getFirstVisibleCharOffset` 探点落元素时直接走几何扫描，不再取该元素子树首个文本节点 + 拿子节点下标当字符数（落在章容器 / body 上会算出章首附近的假偏移）；③ 契约守卫：新 `charOffsetOnCurrentPage(offset)` 走 `scrollToCharOffset` 同款走查定位到字、校验其 rect 与当前页 content-box 沿翻页轴相交，`getLastVisibleCharOffset(start)` 起点不在本页返 -1（先于 `isAtEnd` 钳位——末页起点错时钳到 total 同样是整段幻象），`fushiProgressDetails` 的 atEnd 钳位只盖在已校验的 end 上；Dart `_refreshProgress` 对第四段 -1 不 arrive（原有 `absoluteCharOffsetOf` 负偏移返 -1 已覆盖），诊断流水加 `[ledger] skip [start,?) end unverified` 一行让「这页没计」可见。
- **[x] ② 已加自动化测试** — `fushi/test/reader/paged_first_visible_scan_axis_behavior_test.dart` + `.js`（Node 真跑 `paginatedShellSource()` 的 `fushiReader` 字面量 + 真 `fushiStudyUnits`，伪 DOM 按三页带状网格摆逐字 rect：竖横两模式下扫描兜底数出前页 15 字而不是 0；`charOffsetOnCurrentPage` 只认本页、拒前页 / 后页 / 章首 / 越界；`getLastVisibleCharOffset(0)` 返 -1、`(15)` 返页尾、atEnd 绕不过 -1；探点落元素 / caret null 都走几何扫描；`fushiProgressDetails` 装配 `'100,100,0,-1'`）；既有守卫 `paged_first_visible_scan_fallback_guard_test.dart` 改钉新不变式（首边 = `pagedFirstEdge`、三态短路横竖同式、翻页轴 = 竖排 top/bottom / 横排 left/right、`getLastVisibleCharOffset` 校验先于 `isAtEnd`），`reader_visible_range_shell_guard_test.dart` 改钉 `end = hasEnd ? getLast(off) : -1; if (atEnd && end >= 0) end = total`。
- **备注**：`_charOffsetAtPoint`（页尾探针）保留「元素 → 子树首个文本节点」写法：页尾算错只会 `end <= start` 落进 `exploredCharsBeforeScroll(下一页页首)` 降级（节点粒度、单调），不会产生整段幻象。修复前已入账的幻象字数不回溯修正（`study_segments` 事实表里那 5172 字留着）；用户如需可在统计中心手动删该 session。真机复测（iOS 同书同页）待用户侧验证——本机无该书 22 卷与 iOS 设备。
