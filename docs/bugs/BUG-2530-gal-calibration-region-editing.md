## BUG-2530 · 游戏样本校准缺少区域外框与可修正的标记点
- **报告**：2026-09-18（用户：调整高度看不出变化，希望直接拖框；手动点击字符中心难以精准，换句后出现偏差。）
- **真实性**：✅ 真 bug。第一轮候选 `d047de1706` 的 `gal_lookup_samples_dialog.dart` 只绘制 native 字框与人工十字，未绘制区域外框；区域高度只改变排版空间，不改变字号，因此单行文字未溢出时几乎看不出变化。标记只支持重新点击，无拖动/单点微调；两点拟合也没有足够信息辨认误点。换句偏差的全部来源尚未证实，不能归结为人工误点。
- **[x] ① 已实现，真机待验收** — `fushi/lib/src/pages/implementations/gal_lookup_calibration_canvas.dart:86` 增加独立外框、边角与标记拖动，并保护小于编辑器最小值的旧草稿；`:152` 将浏览缩放与编辑指针分开，避免小幅拖动被手势阈值/竞争影响。`gal_lookup_samples_dialog.dart:706` 区分区域与字框，使用截图像素数值；`:300` 在保存/应用前提交未回车输入，应用前重新检查排版。`fushi/lib/src/lookup/gal_lookup_calibration_draft.dart:265` 在至少六个训练点时降低离群点权重，保留全部标记与偏差显示。
- **[x] ② 已加自动化测试** — `fushi/test/pages/gal_lookup_samples_dialog_test.dart` 覆盖区域拖动/拉底边、缩放坐标与边界、标记拖动/一像素微调、不从中心抓取、模式切换、窄区域、未提交数值保存及应用前拒绝溢出；`fushi/test/lookup/gal_lookup_calibration_draft_test.dart` 覆盖误点、多句小幅噪声与留出样本不参与拟合。两文件的当前用例分轮通过，共 29 项；六个改动 Dart 文件定向 analyze 零诊断。
- **修复与测试提交**：`bea0059f1a`。
- **备注**：用户负责完整 Windows x64 EXE 构建与游戏验收，本轮未执行完整构建或再次扩大测试范围。真实字体、多行排版、后续句查词和分辨率变化尚未验收；本项工程测试不能证明跨句错位已完全解决。只改本机候选，不合并、不推送。
