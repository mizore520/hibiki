<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.fushi.reader">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:label="Fushi"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon_squircle"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        xmlns:tools="http://schemas.android.com/tools"
        tools:replace="android:label">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                    the Android process has started. This theme is visible to the user
                    while the Flutter UI initializes. After that, this theme continues
                    to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <!-- OAuth redirect callback for cloud sync backends
                 (fushi://auth/onedrive, fushi://auth/dropbox). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fushi" android:host="auth" />
            </intent-filter>
        </activity>

        <!-- Launcher icon aliases for runtime icon switching -->
        <activity-alias
            android:name=".MainActivityDefault"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/launcher_icon_squircle"
            android:label="Fushi"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityFushiFull"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/launcher_icon_full"
            android:label="Fushi"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
        </activity-alias>

        <!-- 「透明（无背景）」备选档（TODO-1241）：藏青 wordmark，自适应背景全透明、
             legacy png 也透明。默认档已改用不透明白 squircle（任意壁纸可见），此档保留
             给偏好极简透明观感的用户。 -->
        <activity-alias
            android:name=".MainActivityFushiTransparent"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/launcher_icon_minimal"
            android:label="Fushi"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
        </activity-alias>

        <!-- 退役的「简约」alias（TODO-868 去重：与默认档映射同一张图，是重复选项）。
             不再作为可选预设，但保留声明：老用户若当前启动器指向此 alias 且默认
             alias 已被禁用，删除声明会导致升级后 launcher 图标消失（zero-LAUNCHER）。
             IconSwitchHelper.getCurrentIcon 会在首次读取时把这类老用户安全迁回
             .MainActivityDefault（图标字节相同，无视觉变化）。迁移普及后方可删除。 -->
        <activity-alias
            android:name=".MainActivityFushiMinimal"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/launcher_icon_minimal"
            android:label="Fushi"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
        </activity-alias>

        <!-- 失活：暂留作回退，本轮不删；intent-filter 已迁到 PopupDictFlutterActivity -->
        <activity
            android:name=".PopupDictActivity"
            android:theme="@style/PopupDictTheme"
            android:process=":popup"
            android:taskAffinity="app.fushi.reader.popup"
            android:excludeFromRecents="true"
            android:autoRemoveFromRecents="true"
            android:launchMode="singleTop"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <activity
            android:name=".PopupDictFlutterActivity"
            android:theme="@style/PopupDictTheme"
            android:process=":popup"
            android:taskAffinity="app.fushi.reader.popup"
            android:excludeFromRecents="true"
            android:autoRemoveFromRecents="true"
            android:launchMode="singleTop"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <data android:mimeType="text/plain" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.TRANSLATE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fushi" android:host="lookup" />
            </intent-filter>
        </activity>
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true" tools:ignore="Instantiatable">
        <intent-filter>
            <action android:name="android.media.browse.MediaBrowserService" />
        </intent-filter>
        </service>
        <service android:name=".FloatingLyricService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_lyric_overlay"/>
        </service>
        <service android:name=".FloatingDictService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="clipboard_dictionary_overlay"/>
        </service>
        <!-- 暂时取消无障碍权限的申请：注释掉无障碍服务声明，app 不再出现在
             系统「无障碍」列表里，悬浮词典仅靠剪贴板+手动搜索。后面再决定是否恢复。
             恢复方法：取消本段注释即可（Java/xml/提示文案均保留未删）。
        <service android:name=".DictAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/fushi_dict_accessibility" />
        </service>
        -->
        <service android:name=".FloatingDictTile"
            android:exported="true"
            android:label="Dictionary"
            android:icon="@drawable/ic_stat_fushi"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true" tools:ignore="Instantiatable">
        <intent-filter>
            <action android:name="android.intent.action.MEDIA_BUTTON" />
        </intent-filter>
        </receiver> 
        <provider
            android:name="app.fushi.reader.FushiFileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <provider
            android:name="com.pichillilorenzo.flutter_inappwebview_android.InAppWebViewFileProvider"
            android:authorities="${applicationId}.flutter_inappwebview.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <!-- TODO-1110 / BUG-535：禁用 Flutter 的 Android SurfaceControl 后端。
             media_kit_video 1.3.0 起把 Android 纹理从 SurfaceTextureEntry 迁到
             TextureRegistry.SurfaceProducer。在部分 Android 11 ROM/GPU 驱动
             （如 RMX3085/realme 8）上，SurfaceControl 支撑的 SurfaceProducer 首次
             onSurfaceAvailable 回调不触发 → native wid 永为 0 → android_video_controller
             widListener 永远设 vo=null → mpv 拿不到 GL surface（current-vo/gpu-api 全空、
             texture id 停 null）→ 解码出帧但无画面（黑屏）。参见 flutter/flutter#156488、
             #162902（SurfaceProducer 表面生命周期），媒体渲染最广兼容路径是关掉
             SurfaceControl，让 SurfaceProducer 回落到经典 ImageReader/SurfaceTexture 支撑
             （表面同步可用）。这是 Flutter 官方文档化的后端选择开关，非绕过/掩盖补丁；
             对已正常出画的设备无功能差异（只是不用较新的 SurfaceControl 合成路径）。 -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableSurfaceControl"
            android:value="false" />
        <!-- Don't delete the meta-data below.
                This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Used for AnkiDroid API -->
    <uses-permission android:name="com.ichi2.anki.permission.READ_WRITE_DATABASE"/>
    <queries>
        <package android:name="com.ichi2.anki" />
        <!-- Hibiki→Fushi 迁移：Android 11+ 包可见性，不声明则 getPackageInfo 查不到新包 -->
        <package android:name="app.hibiki.reader" />
    </queries>
</manifest>
