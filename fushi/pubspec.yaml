name: fushi
description: A focused Japanese EPUB reader with popup dictionary and Anki integration.
publish_to: 'none'
version: 2.1.1+1218
environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: "^3.41.6"

dependencies:
  archive: ^3.6.1
  async_zip: ^0.1.0
  basic_utils: ^5.7.0
  audio_service: ^0.18.9
  audio_session: ^0.1.13
  cached_network_image: ^3.4.1
  carousel_slider: ^4.2.1
  change_notifier_builder: ^1.1.2
  characters: ^1.3.0
  clipboard: ^0.1.3
  collection:
  cupertino_icons: ^1.0.8
  crop_image: ^1.0.4
  desktop_drop: ^0.5.0
  device_info_plus: ^9.0.0
  dio: ^5.1.1
  dynamic_color: ^1.7.0
  external_app_launcher: ^4.0.3
  external_path: ^2.2.0
  # 分段条溢出滚动的渐隐边缘（vendored，见 dependency_overrides）。
  fading_edge_scrollview: ^3.0.0
  file_picker: ^8.0.0
  float_column: ^4.0.3
  # Cross-platform game controllers (GameInput / GameController / evdev).
  # BUILD PREREQ: the Windows backend (gamepads_windows) #includes <GameInput.h>,
  # which ships only in Windows SDK >= 10.0.26100 — every Windows build
  # machine/CI must have that SDK installed. (Android is overridden to a no-op
  # below; see dependency_overrides.)
  gamepads: ^0.1.10+2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  fushi_core:
    path: ../packages/fushi_core
  fushi_dictionary:
    path: ../packages/fushi_dictionary
  fushi_anki:
    path: ../packages/fushi_anki
  fushi_audio:
    path: ../packages/fushi_audio
  fushi_platform:
    path: ../packages/fushi_platform
  fushi_torrent:
    path: ../packages/fushi_torrent
  google_sign_in: ^6.2.2
  extension_google_sign_in_as_googleapis_auth: ^2.0.12
  googleapis: ^13.2.0
  googleapis_auth: ^1.6.0
  flutter_archive: ^5.0.0
  flutter_cache_manager: ^3.3.0
  flutter_charset_detector: ^1.0.2
  flutter_colorpicker: ^1.1.0
  flutter_markdown: ^0.6.18
  flutter_exit_app: ^1.0.5
  flutter_html: ^3.0.0
  flutter_inappwebview: ^6.1.5
  flutter_logs: ^2.1.11 
  flutter_riverpod: ^2.3.6
  flutter_staggered_grid_view: ^0.7.0
  fluttertoast: ^8.2.1
  drift: ">=2.33.0 <2.34.0"
  sqlite3_flutter_libs: ^0.5.28
  sqlite3: ">=2.9.0 <4.0.0"
  ffi: ^2.1.3
  gap: ^3.0.1
  http: ^1.2.0
  html: ^0.15.2
  html_unescape: ^2.0.0
  youtube_explode_dart: ^2.3.6
  image: ^4.3.0
  image_picker: ^0.8.7
  intl: ^0.20.2
  json_annotation: ^4.8.1
  json_serializable: ^6.6.1
  just_audio: ^0.9.31
  just_audio_media_kit: ^2.1.0
  # media_kit_libs_windows_audio 移除：与视频用的 media_kit_libs_video 抢同名 libmpv
  # 归档(同下载路径不同 MD5)，video libs 是 audio 超集，统一提供 libmpv。
  kana_kit: ^2.0.0
  macos_ui: ^2.2.2
  material_color_utilities: ^0.13.0
  marquee: ^2.2.3
  multi_value_listenable_builder: ^0.0.2
  network_to_file_image: ^4.0.1
  package_info_plus: ^4.0.2
  path: ^1.8.2
  path_provider: ^2.0.13
  permission_handler: ^10.2.0
  # PDF 阅读器（Phase 0 spike）：PDFium 底层、字符级文本矩形供点选查词。
  # 五平台 + native-assets/build-hook 下载 PDFium。见 docs/specs/pdf-reader-lookup。
  # 钉 2.4.5：>=2.4.6 经 pdfrx_engine 拉 image ^4.8.0 → 要 archive ^4.0.7 / xml ^7.0.1，
  # 与本仓 archive ^3.6.1 / xml ^6.3.0 冲突（升 archive/xml 主版本代价大，Phase 0 先钉）。
  pdfrx: 2.4.5
  progress_indicators: ^1.0.0
  recase: ^4.1.0
  receive_intent:
    git:
      url: https://github.com/arianneorpilla/receive_intent
      ref: 3854d07f8f4e9d5a78589639b18b5d454ddf54a0
  remove_emoji: ^0.0.7
  restart_app: ^1.1.2
  share_plus: ^7.2.2
  shared_preferences: ^2.2.2
  slang: ^3.13.0
  slang_flutter: ^3.13.0
  sliver_tools: ^0.2.12
  stack_trace: ^1.11.1
  shelf: ^1.4.0
  ftpconnect: ^2.0.10
  dartssh2: ^2.9.0
  crypto: ^3.0.0
  meta: ^1.9.0
  transparent_image: ^2.0.1
  url_launcher: ^6.0.20
  visibility_detector: ^0.4.0+2
  xml: ^6.3.0
  wakelock_plus: ^1.1.4
  record: 6.0.0
  brotli: ^0.6.0
  bonsoir: ^7.1.1
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_video: ^1.0.7
  # 移动端 ffmpeg：用「自编」的 ffmpeg-kit（arthenica 源码 + NDK r25 重编最小变体），
  # 替代崩溃的第三方预编译 ffmpeg-kit 变体（其 libffmpegkit_abidetect.so 在
  # Android 16/API 36 上 JNI_OnLoad 返回非法版本，启动即崩，BUG-122）。vendored 包见
  # dependency_overrides 的 ffmpeg_kit_flutter（android 用 third_party 自编 AAR）。
  ffmpeg_kit_flutter: ^6.0.3
  web_socket_channel: ^3.0.0
  # 漫画内置 OCR：ONNX Runtime 五平台绑定（检测 RT-DETR + 识别 manga-ocr）。
  # 选型见 lib/src/ocr/；EP 策略（CUDA/DirectML/CoreML/CPU）在 ocr_inference.dart。
  flutter_onnxruntime: ^1.8.3
  clipboard_watcher: ^0.3.0
  hotkey_manager: ^0.2.3
  screen_retriever: ^0.2.0
  window_manager: ^0.5.1
dependency_overrides:
  # TODO-1306 / TODO-1275 / BUG-361: vendored desktop_drop 0.5.0. Adds (a) URL
  # drag import — browsers drop a URL as CFSTR_INETURLW (Windows) / public.url
  # (macOS) / an http(s) line in text/uri-list (Linux), all of which upstream
  # discards, so a dragged link never reaches Dart; the fork extracts the URL and
  # sends it through the same channel as file paths for the classifier to import
  # as a stream video — and (b) the re-register-OLE-drop-target `reinitialize`
  # method (BUG-361). Was a pub-cache ci-patch; vendored so the cross-platform
  # native delta is tracked source. See third_party/desktop_drop/PATCHES.md.
  desktop_drop:
    path: ../third_party/desktop_drop
  # 自编 ffmpeg-kit（arthenica 源码重编，android 用 third_party 内自编 AAR；修 BUG-122
  # Android16 启动崩溃）。Dart API 与上游 ffmpeg_kit_flutter 6.0.3 一致。
  ffmpeg_kit_flutter:
    path: ../third_party/ffmpeg_kit_flutter
  flutter_inappwebview_windows:
    path: ../packages/flutter_inappwebview_windows
  # flutter_inappwebview_android 1.1.3 has legacy openFileChooser overloads
  # declared protected, which fails against the current Android WebChromeClient
  # API at compileSdk 36. Keep the Android implementation vendored until an
  # upstream release carries the public-method fix.
  flutter_inappwebview_android:
    path: ../third_party/flutter_inappwebview_android
  # Android uses the engine's native key-event path for controllers, NOT the
  # gamepads plugin. The real gamepads_android crashes at launch (it casts
  # MainActivity to GamepadsCompatibleActivity unconditionally), so override it
  # with a Dart-only no-op so no native ActivityAware plugin is registered.
  gamepads_android:
    path: ../packages/gamepads_android_stub
  # BUG-116: the pub plugin gamepads_windows 0.3.0+1 crashes — its polling
  # thread is detached and keeps calling GetCurrentReading on g_gameInput AFTER
  # stop() Releases it (teardown use-after-free at FlutterDesktopViewController
  # Destroy), emits events by calling the Flutter MethodChannel directly off the
  # polling thread (channel must be platform-thread only), races on plain-bool
  # flags, and registers its device callback through an uninitialized wild
  # pointer. Vendored fork fixes all four (join-then-release threads, marshal
  # events to the platform thread via a message-only window, atomic flags,
  # value-typed callback token). Keep until upstream carries the fix.
  gamepads_windows:
    path: ../packages/gamepads_windows
  # Vendored third-party packages carrying Flutter 3.x API-compat patches that
  # are not published on pub.dev. Previously these lived only as hand-edits in
  # each dev's pub-cache, so a clean checkout failed to build (e.g. on iOS):
  #   network_to_file_image: hashValues→Object.hash, DecoderCallback /
  #     PaintingBinding.instantiateImageCodec / ImageProvider.load removals.
  #   carousel_slider: CarouselController name clash with flutter/material.
  #   fading_edge_scrollview: PageController vs ScrollController type.
  # Source under third_party/ (SDK upper bound bumped to <4.0.0 for Dart 3).
  network_to_file_image:
    path: ../third_party/network_to_file_image
  carousel_slider:
    path: ../third_party/carousel_slider
  fading_edge_scrollview:
    path: ../third_party/fading_edge_scrollview
  # BUG-073: upstream media_kit_libs_windows_video 1.0.11 pins a 2023-09-24
  # libmpv whose FFmpeg build cannot decode TrueHD (TrueHD 5.1 movies play with
  # NO sound — verified the bundled DLL fails `Opening decoder truehd` at
  # runtime). Vendored fork repoints the CMake download to a maintained
  # full-FFmpeg libmpv (zhongfly/mpv-winbuild) that decodes TrueHD; see
  # third_party/media_kit_libs_windows_video/windows/CMakeLists.txt. Same DLL
  # also backs the audiobook player, so this fixes TrueHD audiobooks too.
  media_kit_libs_windows_video:
    path: ../third_party/media_kit_libs_windows_video
  # BUG-073 (cont.): the SAME TrueHD gap exists on macOS/iOS/Android — media-kit's
  # "default" FFmpeg flavor enables the truehd demuxer but not its decoder, so
  # TrueHD audio is silent on every platform, not just Windows. These forks switch
  # each platform to the "full" flavor (which enables all decoders). Linux uses
  # the system libmpv (full FFmpeg) and needs no override.
  #
  # TODO-1137: every media-kit release, full flavor included, is built against
  # FFmpeg 6.0 (2023-02) — off the maintenance branches, no security backports,
  # and this is the library that decodes whatever file the user opens. All three
  # are therefore built from our own forks at FFmpeg 6.1.6 (6.1 LTS tip) and
  # mirrored into the permanent `vendor-libmpv` release: Android from
  # libmpv-android-video-build, macOS/iOS from libmpv-darwin-build, both on the
  # `hibiki/ffmpeg-6.1.6` branch. Staying inside 6.x keeps the FFmpeg API stable,
  # so mpv itself is unchanged (7.x would force a mpv bump, which in turn makes
  # libplacebo a hard dependency neither build repo has).
  # See each package's Makefile / build.gradle for the repoint + checksums.
  media_kit_libs_macos_video:
    path: ../third_party/media_kit_libs_macos_video
  media_kit_libs_ios_video:
    path: ../third_party/media_kit_libs_ios_video
  media_kit_libs_android_video:
    path: ../third_party/media_kit_libs_android_video
  # BUG-235: media_kit_video 2.0.1's MaterialDesktopSeekBarState.onPointerUp /
  # onPointerMove call controller(context) (dereferences State.context) with no
  # mounted guard. Hibiki tears down the controls subtree on fullscreen
  # enter/exit and episode switch (VideoControlsFocusGate), so releasing the
  # seek bar drag right then crashes with "Null check operator used on a null
  # value". Vendored fork adds `if (!mounted) return;` to both handlers. See
  # third_party/media_kit_video/PATCHES.md.
  media_kit_video:
    path: ../third_party/media_kit_video
  # macOS build fix: upstream flutter_onnxruntime 1.8.3 declares ios/macos plugin
  # platforms whose podspecs pin onnxruntime-objc 1.23 -> forces macOS 14 / iOS 16
  # deployment target, breaking the "Build Desktop" macOS release job (project is
  # macOS 10.15). Vendored fork drops the ios/macos plugin-platform declarations
  # (+ deletes the Apple native trees) so Flutter never registers it into the
  # macOS Swift package; android/linux/windows native OCR is untouched. Apple's
  # manga OCR degrades to interconnect-host / Gemini cloud (isLocalOnnxRuntimeAvailable).
  # See third_party/flutter_onnxruntime/PATCHES.md.
  flutter_onnxruntime:
    path: ../third_party/flutter_onnxruntime

dev_dependencies:
  build_runner: ^2.3.3
  just_audio_platform_interface: ^4.6.0
  # TODO-817 M1c scanner test overrides CharsetDetectorPlatform with a Dart
  # fake to verify SJIS subtitle decoding headlessly (native channel is
  # unavailable in flutter test). These are transitive deps of
  # flutter_charset_detector; declared here only for the test import.
  flutter_charset_detector_platform_interface: ^1.1.0
  plugin_platform_interface: ^2.1.0
  # BUG-1213: android_permission_service_test 用它替换
  # PermissionHandlerPlatform.instance，在 flutter test 里复刻
  # permission_handler_android 的 SDK 分级语义（原生通道不可用）。
  permission_handler_platform_interface: ^3.9.0
  drift_dev: ^2.23.0
  dependency_validator: ^3.0.0
  flutter_lints: ^2.0.1
  flutter_native_splash: ^2.2.8
  fake_async: ^1.3.1
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    # TMDB 官方 logo（合约署名素材）。逐文件登记而非整目录，是为了只打包实际渲染
    # 的 PNG，把同目录的矢量原图 blue_square_1.svg（provenance 真相源）与 README.md
    # （来源/哈希/禁改说明 + 栅格化配方）留在仓库里、不进应用包。
    - assets/attribution/tmdb/logo_tmdb.png
    - assets/browser_extension/
    - assets/browser_extension/vendor/
    - assets/gamepad/xbox/
    - assets/gamepad/playstation/
    - assets/gamepad/switch/
    - assets/licenses/
    - assets/meta/
    - assets/popup/
    - assets/transforms/
