# ffigen 配置：从 native/fushi_torrent 的 C ABI 头文件生成 Dart FFI 绑定。
# 生成命令（在本包目录下）：dart run ffigen --config ffigen.yaml
# 需要本机装有 LLVM/libclang；若缺，绑定文件与本配置产物等价的手写版已入库
# （lib/src/ffi/fushi_torrent_bindings.dart 顶部有说明）。
name: FushiTorrentBindings
description: Dart FFI bindings for the fushi_torrent C ABI bridge over libtorrent.
output: "lib/src/ffi/fushi_torrent_bindings.dart"
headers:
  entry-points:
    - "../../native/fushi_torrent/fushi_torrent_include/fushi_torrent.h"
  include-directives:
    - "**/fushi_torrent.h"
functions:
  include:
    - "ht_.*"
preamble: |
  // GENERATED / hand-mirrored — do not edit by hand except to re-run ffigen.
  // ignore_for_file: always_specify_types, camel_case_types, non_constant_identifier_names
comments:
  style: any
  length: full
